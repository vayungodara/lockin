# Coverage Report — 2026-07-19

## Summary

| Metric | Before | After |
|--------|--------|-------|
| Statements | 89.56% | 93.71% |
| Branches | 77.41% | 82.69% |
| Functions | 79.41% | 97.05% |
| Lines | 92.49% | 95.22% |

Total tests: 265 → 301 (+36 new tests).

## Week-over-Week (vs 2026-06-21)

| Metric | 2026-06-21 | 2026-07-19 | Change |
|--------|-----------|-----------|--------|
| Statements | 89.56% | 93.71% | +4.15% |
| Branches | 77.41% | 82.69% | +5.28% |
| Functions | 79.41% | 97.05% | +17.64% |
| Lines | 92.49% | 95.22% | +2.73% |
| Test count | 265 | 301 | +36 |

Files that were under-covered last report and are now fully covered:
- `lib/notifications.js` — 87.27% → 100% (statements), 85.29% → 100% (branches)

Files with improved coverage:
- `lib/activity.js` — 77.58% → 97.41% (statements), 29.16% → 100% (functions)
- `lib/streaks-advanced.js` — 84.12% → 88.09% (statements), 74.11% → 80% (branches)

Files that remain below 80% on any metric:
- `lib/streaks-advanced.js` — 88.09% stmts but uncovered lines in `updateStreakOnCompletion` dynamic import paths (lines 347, 355-356, 377)

## Files Changed (last 7 days)

No `lib/` files were modified in the last 7 days. Fell back to lowest-coverage files overall.

## New Tests Written

- `tests/unit/lib/notifications.test.js` — extended with 17 new tests, covers: getNotifications (happy path, null data, DB error, custom limit, default limit), getNotificationIcon (all 13 remaining type-specific icon assertions)
- `tests/unit/lib/activity.test.js` — extended with 9 new tests, covers: getGroupActivity (profile attachment, missing profiles), getAllActivity (test data filtering, limit trimming, profile/reactions attachment), getGroupStats (completion rate calculation, leaderboard sorting, null data handling)
- `tests/unit/lib/streaks-advanced.test.js` — extended with 10 new tests, covers: FREEZE_COOLDOWN_DAYS constant, awardStreakFreeze (profile not found, capped at max, RPC success, RPC failure, fallback remaining), getStreakFreezeStatus nextFreezeEarned (milestone hints at 7/14/30, past all milestones, error defaults)

## Skipped (out of scope)

- `lib/FocusContext.js`, `lib/NotificationContext.js`, `lib/KeyboardShortcutsContext.js` — React context providers
- `lib/animations.js` — pure animation data presets, no logic to test
- `lib/supabase/*` — Supabase client configuration
- `lib/confetti.js` — relies on DOM APIs (createElement, requestAnimationFrame, performance.now)
- `lib/sounds.js` — relies on Web Audio API (AudioContext)
- `lib/email.js` — core logic in non-exported helper functions; only export requires real Resend client
- `lib/useKeyboardShortcuts.js`, `lib/useModalScrollLock.js` — React hooks, out of scope

## Notes

- `activity.js` went from 29.16% to 100% function coverage by testing `getGroupActivity`, `getAllActivity`, and `getGroupStats` with sequenced mock responses. The remaining branch gaps (80.89%) are in code paths where `data` is null after an initial check — the `(data || [])` fallbacks execute but the branch condition itself doesn't flip.
- `notifications.js` reached 100% on all metrics by adding the `getNotifications` function test (previously imported but never called in tests) and explicit assertions for all `getNotificationIcon` switch cases.
- `streaks-advanced.js` lines 347, 355-356, and 377 remain uncovered — these are inside dynamic `import('@/lib/notifications')` and `import('@/lib/gamification')` calls within `updateStreakOnCompletion` and `checkStreakMilestone`. Testing these requires mocking ES dynamic imports at the module level (e.g., `vi.mock`), which would add complexity for minimal coverage gain since the try/catch ensures they fail safely.
