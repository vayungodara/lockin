# LockIn Morning Triage — 2026-03-29

**Generated:** 2026-03-29 (automated)
**Findings:** 11 from bug-hunter + 9 Cowork visual findings enriched = 20 total
**Cross-referenced:** 1 carried over, 1 already fixed, 0 cannot verify
**Cowork visual findings enriched:** 9

---

## Progress Since Yesterday
- Items from yesterday: 2 total, 1 fixed (CreatePactModal deadline validation), 1 still open (ActivityFeed race condition)
- Commits since yesterday: 2 (fix: past-deadline validation, fix: resolve findings from Mar 25-27 triage)
- Cowork visual findings available: yes (15 findings, 9 visual bugs enriched with file paths)
- DB health: stable (531 rows, +6 from yesterday), XP drift unchanged

## Vercel Health
- All deployments: Ready (no failures in last 48h)
- All routes returning 200 (no 500s or timeouts)
- Production domain: www.lock-in.me

---

## Already Fixed

| File | Line | Description |
|------|------|-------------|
| components/CreatePactModal.js | L148-151 | Deadline date validation — past-deadline check added in commit 4ce1940 |

---

## Still Open — Carried Over

| Severity | File | Line | Description | Fix Suggestion |
|----------|------|------|-------------|----------------|
| high | components/ActivityFeed.js | L104-124 | `loadMore` race condition — IntersectionObserver can fire two rapid calls reading same `activities.length`, producing duplicate pages. No abort controller or dedup. Same class of bug as yesterday's L46 finding. | Use a ref for offset, deduplicate by `id` in `setActivities`, or add AbortController |

---

## Still Open — New

| Severity | File | Line | Description | Fix Suggestion |
|----------|------|------|-------------|----------------|
| high | components/CreateGroupModal.js | L12-18 | `generateInviteCode` uses `Math.random()` for 6-char codes. Not cryptographically secure; keyspace 36^6 brutable at scale. | Use `crypto.getRandomValues()` or increase to 8+ characters |
| high | app/dashboard/focus/FocusPageClient.js | L38-55 | "All Time" stat sums only latest 10 sessions (`.limit(10)` at L43). Shows 0h when user has 6h+ total. Cowork finding LI-3 root cause. | Remove limit for stats query or run a separate COUNT query |
| medium | app/dashboard/stats/StatsPageClient.js | L73-78 | Focus sessions query uses `.select('*')` with no `.limit()` or date filter — unbounded for users with many sessions | Add `.limit(500)` or select only needed fields |
| medium | components/FocusTimer.js | L71-73 | SVG gradient hardcodes `#6366F1`, `#8B5CF6`, `#D946EF` — breaks with non-indigo accent palettes | Use CSS variables via stop-color or SVG styling |
| medium | components/LandingPageClient.js | L399 | Hardcoded hex color array for decorative dots duplicates brand colors | Extract to constant or use CSS variables |
| medium | components/CompactActivityCard.js | L138-144 | Inline Framer Motion variants instead of presets from `@/lib/animations` | Use existing animation presets |
| low | app/join/[code]/page.js | L12-37 | Error/invalid states use extensive inline `style={{...}}` instead of CSS Modules | Create shared error component or CSS module |

### Cowork Visual Findings (enriched with file paths)

| ID | Severity | Finding | File | Fix |
|----|----------|---------|------|-----|
| LI-1 | high | Desktop challenge card renders empty | components/OnboardingChecklist.js | No desktop-specific CSS hiding found; may be state/timing issue with Framer Motion opacity animation. Needs manual investigation. |
| LI-2 | high | Light mode text contrast failures | app/globals.css L29 | `--text-muted: #A9A5BC` has ~2.7:1 contrast ratio (fails WCAG AA). Darken to ~#706C8A for 4.5:1. |
| LI-3 | high | Focus Timer "0h ALL TIME" vs Stats "6h 23m" | app/dashboard/focus/FocusPageClient.js L43 | Root cause: `.limit(10)` on session query means "All Time" only sums 10 latest sessions |
| LI-4 | medium | Sidebar truncated labels during collapse | components/Sidebar.js L256-267, Sidebar.module.css L200 | Framer Motion spring transition (200/25) shows intermediate text clipping. Needs faster opacity fade or instant hide. |
| LI-5 | medium | Calendar dates invisible in dark mode | components/MonthlyCalendar.module.css L259-301 | Hardcoded rgba level colors need `[data-theme="dark"]` overrides with boosted opacity |
| LI-6 | medium | Settings missing from mobile nav | components/MobileNav.js L9-63 | `navItems` has 5 items (no Settings). Add 6th item or "More" menu. |
| LI-7 | low | Duplicate streak display on Stats | app/dashboard/stats/StatsPageClient.js L225-249 + components/MonthlyCalendar.js L187-209 | Remove one of the two streak displays |
| LI-8 | low | Inconsistent checkmark icon in Stats | app/dashboard/stats/StatsPageClient.js L243 | **FIXED** — replaced plain ✓ with ✅ emoji to match fire/trophy style |
| LI-9 | low | Focus Analytics empty space | app/dashboard/stats/StatsPageClient.js L298-321 | Add mini chart or breakdown section to match Pact Analytics card |

---

## Automated Fixes Applied

| File | Change | Lines Modified |
|------|--------|----------------|
| components/CreateGroupModal.js | Remove unused `modalOverlay` import | 1 |
| components/CreateTaskModal.js | Remove unused `modalOverlay` import | 1 |
| app/dashboard/focus/FocusPageClient.js | Add `user.id` to `fetchSessions` useCallback dependency array | 1 |
| app/dashboard/pacts/PactsPageClient.js | Change dependency from `user` to `user.id` in useCallback | 1 |
| app/dashboard/stats/StatsPageClient.js | Replace plain ✓ with ✅ emoji for icon consistency | 1 |

---

## Skipped — Needs Manual Review

- components/ActivityFeed.js:L104 — Race condition fix requires restructuring async data flow (abort controller + dedup), exceeds 30-line easy fix scope
- components/CreateGroupModal.js:L12-18 — Invite code security fix requires crypto API + testing, touches auth-adjacent code
- app/dashboard/focus/FocusPageClient.js:L38-55 — "All Time" data bug requires query restructuring (separate unlimited query for total stats)
- app/globals.css:L29 — Light mode contrast fix (darken --text-muted) affects entire site, needs visual review across all pages
- components/MobileNav.js — Adding Settings to mobile nav requires UX decision (6th item vs "More" menu)
- components/MonthlyCalendar.module.css — Dark mode overrides need testing across all 7 accent palettes
