# LockIn Morning Triage — 2026-03-24

**Generated:** 2026-03-24 08:00 CET (automated)
**Sources:** Morning Report A: present, Morning Report B: present, Frontend Audit: present
**Findings before dedup:** 16 from A + 2 from B = 18 total before merge
**After dedup:** 18 merged findings (no overlaps)
**Cross-referenced:** 18 verified against repo, 7 already fixed, 3 cannot verify

---

## Already Fixed

> These issues were flagged in today's reports but are no longer present in the current code.

| File | Line | Description |
|------|------|-------------|
| components/OnboardingChecklist.js | L164-187 | Debounce timer leak — timer variable is properly scoped within the useEffect closure and cleaned up on unmount |
| components/ActivityFeed.js | L52-77 | Activity feed duplicate loads — guarded by `isLoadingMoreRef` and `loadMoreRef` pattern prevents concurrent loads |
| lib/activity.js | L90-96 | Activity reactions query per page — already uses `getBatchReactions()` with `.in()` for batch fetching |
| lib/FocusContext.js | L283-295 | FocusContext value recreated on every state change — value is wrapped in `useMemo()` with proper dependencies |
| components/OnboardingChecklist.js | L59 | Dead isPreviewRef — the ref IS actively used at L78 (guard check) and L151 (set on preview) |
| lib/streaks.js | L36-43 | Timezone string not validated — `formatDateInTimezone()` uses try/catch and falls back to UTC for invalid timezones |
| lib/NotificationContext.js | L13 | Supabase client memo pattern unusual — `useMemo(() => createClient(), [])` is the standard pattern used across the codebase |

---

## Still Open — Carried Over

> Present in today's report AND yesterday's triage. These have persisted at least 2 days.

No carried-over issues (first run).

---

## Still Open — New Since Yesterday

> Present in today's report but NOT in yesterday's triage (first run — all marked new).

| Severity | File | Line | Description | Fix Suggestion |
|----------|------|------|-------------|----------------|
| high | lib/reactions.js | L59-60 | `getBatchReactions()` calls `getUser()` without checking error — userId becomes undefined on auth failure, user's own reactions not highlighted | Add `error` destructuring: `const { data: authData, error: authError } = ...` and throw on error, or document graceful degradation as intentional |
| high | lib/partnerships.js | L48-55, L93-100, L231-238 | `createNotification()` return values ignored in 3 places — notification failures are silently swallowed | Check return value of `createNotification()` and log failures, or document that notification failures are non-fatal by design |
| high | app/api/cron/check-streak-breaks/route.js | L79-91 | Batch inserts for notifications and activity_log only `console.error` on failure — cron returns 200 regardless | Return 500 or 207 when batch inserts fail. Add error aggregation to response body |
| medium | lib/partnerships.js | L41-45, L86-90 | Profile `.single()` calls destructure away error — if profile fetch fails, name defaults to "Someone" silently | Add error logging: `const { data: profile, error: profileError } = ...` and log profileError |
| medium | lib/email.js | L56 | ~~userName not HTML-escaped in email template while pactTitle is escaped~~ **FIXED in this PR** — wrapped with `escapeHtml(userName)` | ~~Wrap with `escapeHtml(userName)`~~ Done |
| medium | lib/partnerships.js | L201-243 | `notifyPartner()` accepts userId as parameter without verifying caller is authenticated — relies on caller for auth | Add `getUser()` check at function start, or document that callers must pre-authenticate |
| low | lib/activity.js | L16 | `logActivity()` accepts metadata object without validation — arbitrary data inserted into activity_log | Add basic shape validation or allowlist for metadata keys |
| low | lib/activity.js | L67-82, L124-138, L212-224 | Profile-fetching logic duplicated 3 times with identical pattern (fetch IDs, query profiles, build map) | Extract a shared `fetchProfilesMap(supabase, userIds)` helper |

---

## Cannot Verify

> Issues that could not be confirmed or denied due to missing files, vague descriptions, or out-of-range lines.

| File | Issue | Reason |
|------|-------|--------|
| (multiple lib/ files) | Inconsistent error return patterns across lib/ | Too vague — no specific file or line referenced, multiple patterns coexist by design |
| app/dashboard/groups/[id]/GroupDetailClient.js | Missing real-time subscriptions for live task/activity updates | This is a feature request (adding Supabase realtime channels), not a bug in existing code |
| lib/FocusContext.js | Focus timer state doesn't survive page refresh | This is a feature request (localStorage persistence), not a bug — current behavior is by design |

---

## Frontend Audit Suggestions

> Design and CSS recommendations from today's frontend audit email. Not bugs — improvement ideas.
> **Frontend is the developer's favorite area — go in depth here.**

| Component / File | Suggestion | Priority | Current Code | Recommended Change |
|-----------------|------------|----------|--------------|-------------------|
| components/LandingPageClient.js + app/page.module.css:136 | Replace screenshot placeholder with real dashboard image | high | `.screenshotPlaceholder { height: 400px; background: var(--bg-secondary); }` — a gray box saying "Dashboard preview coming soon" | Take a real screenshot of the dashboard in dark mode at 1800x1000px, save as `/public/dashboard-preview.png`, use `<Image src="/dashboard-preview.png" ... />` with `border-radius: var(--radius-lg)`. This is the single biggest credibility boost for new visitors. Every premium SaaS (Linear, Vercel, Notion) shows real product UI. |
| app/page.module.css:262 | Break the landing page feature grid symmetry | high | `.featureGrid { grid-template-columns: repeat(3, 1fr); }` — uniform 3x2 grid that looks AI-generated | Make the first feature card span 2 columns (hero feature). Use asymmetric grid: `grid-template-columns: 2fr 1fr` for row 1, `repeat(2, 1fr)` for row 2, `1fr 2fr` for row 3. Add subtle SVG background illustrations inside larger cards. Reference: Notion's feature grid varies card sizes with screenshots inside each card. |
| PactsPage.module.css, GroupsPage.module.css, StatsPage.module.css | Add personality and illustrations to empty states | high | Generic dashed-border boxes with gray circle icons, no brand voice | Create 3 unique SVG illustrations (rocket for Pacts, people for Groups, chart for Stats). Replace dashed border with `border: solid var(--border-subtle)`. Add brand-voice copy like "No pacts yet? Your future self is judging you." Use `ambientFloat` animation from `@/lib/animations` on the illustration. Background: `var(--gradient-subtle)`. Reference: Raycast and Linear use unique illustrations per empty state. |
| CreatePactModal.module.css, CreateGroupModal.module.css | Add micro-interactions to form inputs | high | Inputs have basic focus border change only — `border-color: var(--accent-primary)` on `:focus` | Add to `@/lib/animations`: `inputFocus = { animate: { scale: 1.01 }, transition: snappySpring }`, `errorShake = { animate: { x: [0, -4, 4, -4, 0] }, transition: { duration: 0.3 } }`, `successCheck = { initial: { scale: 0, opacity: 0 }, animate: { scale: 1, opacity: 1 }, transition: bouncySpring }`. Add label float animation on focus. Add green checkmark SVG on valid fields. Add `fadeInUp` entrance for validation messages. Reference: Amie's form inputs. |
| app/dashboard/DashboardClient.js | Promote streak counter to dashboard hero position | high | Streak counter is inside DailySummaryCard as one stat among several | Move streak to a prominent hero position at top of dashboard. Display count in `var(--text-4xl)` with large fire emoji. Apply `streakCelebration` animation from `@/lib/animations` on milestone days (7, 14, 30, etc.). Use `celebrationGlow` for the background on milestone days. This reinforces the core product loop every session. |
| app/dashboard/Dashboard.module.css | Fix hardcoded RGBA colors in streak card | medium | `background: rgba(255, 140, 50, 0.08)`, `color: rgba(255, 120, 40, 0.85)` — hardcoded orange values. Also in FocusPage.module.css: `rgba(139,92,246,0.15)` | Replace with CSS variable equivalents: `background: rgba(var(--accent-celebration-rgb, 255, 140, 50), 0.08)`, `color: var(--warning)`. This ensures streak colors adapt to theme and accent palette changes. For FocusPage, use `rgba(var(--accent-primary-rgb), 0.15)`. |
| app/page.module.css:42 | Increase landing page hero title size | medium | `.heroTitle { font-size: clamp(3rem, 6vw, 4.5rem); font-weight: 700; }` | `.heroTitle { font-size: clamp(3.25rem, 7vw, 5rem); font-weight: 800; letter-spacing: -0.04em; }` — bump to extrabold, tighten tracking. 2026 trend: oversized typography. Reference: Arc Browser's massive bold headlines. |
| Multiple CSS modules | Vary border-radius by component hierarchy | medium | Nearly every card uses `var(--radius-2xl)` (20px) uniformly | PactCard → `var(--radius-xl)` (16px), ActivityItem → `var(--radius-lg)` (12px), TaskCard → `var(--radius-lg)`, Toast → `var(--radius-lg)`. Keep modals at `var(--radius-2xl)`. Consider adding new tokens to globals.css: `--radius-card: 16px`, `--radius-item: 12px`. This creates visual hierarchy where larger containers have larger radius. Reference: Linear uses this pattern. |
| app/dashboard/pacts/PactsPage.module.css | Add animated filter tab transitions | medium | Active filter tab just swaps background color instantly | Use Framer Motion `layoutId` on the active indicator pill (like Navbar.js already does for nav links). Add `transition: background var(--transition-base)` to filter buttons. Add `AnimatePresence` to pact card list so cards animate in/out on filter change using `fadeInUp` preset from `@/lib/animations`. |
| components/Toast.js + Toast.module.css | Add pause-on-hover and progress bar to toasts | low | Auto-dismiss at 4s/6s, no hover interaction, no visual countdown | Add a thin progress bar at bottom (height: 2px) using `var(--success)`/`var(--danger)`/`var(--warning)`/`var(--info)` per type. Add to `@/lib/animations`: `toastProgress = { initial: { scaleX: 1 }, animate: { scaleX: 0 }, transition: { duration: 4, ease: 'linear' } }`. Pause countdown on hover via `onMouseEnter`/`onMouseLeave`. Add `cursor: pointer` for click-to-dismiss. Reference: Sonner toast library. |
| globals.css | Add new design system CSS variables and animation presets | low | Current variables cover most needs but missing spring/smooth transitions and component-specific radii | Add to globals.css: `--transition-spring: cubic-bezier(0.34, 1.56, 0.64, 1)`, `--transition-smooth: cubic-bezier(0.22, 1, 0.36, 1)`, `--radius-card: 16px`, `--radius-item: 12px`, `--shadow-card-hover` with border combo. Add to lib/animations.js: `filterTabSlide`, `inputFocus`, `errorShake`, `successCheck`, `toastProgress` presets. |

**Overall Frontend Score (baseline):** 7.3/10 — Strong design system foundation. Top priorities: replace screenshot placeholder, break feature grid symmetry, add empty state personality.

---

## Automated Fixes Applied

> Easy fixes that were committed in branch `fix/morning-triage-2026-03-24`.

| File | Change | Lines Modified |
|------|--------|----------------|
| lib/email.js | Escape `userName` with `escapeHtml()` in email template to prevent XSS — `pactTitle` was already escaped but `userName` was not | 1 |

---

## Skipped — Needs Manual Review

> Issues that qualify for a fix but were skipped because they exceed the automated fix criteria.

- lib/reactions.js:59-60 — Unclear whether graceful degradation on auth failure is intentional; changing behavior could break unauthenticated reaction viewing
- lib/partnerships.js:48-55,93-100,231-238 — Notification error handling is a design decision (fail silently vs. propagate); touches business logic
- app/api/cron/check-streak-breaks/route.js:79-91 — Cron route excluded from automated fixes per policy
- lib/partnerships.js:201-243 — Auth check in `notifyPartner()` requires understanding caller context; touches auth logic
- lib/activity.js:67-82,124-138,212-224 — Profile-fetching deduplication is a refactoring task exceeding scope
