# LockIn Morning Triage — 2026-03-23

**Generated:** 2026-03-23 08:00 CET (automated)
**Sources:** Morning Report A: present, Morning Report B: present, Frontend Audit: present
**Findings before dedup:** 7 from A + 4 from B = 11 total before merge
**After dedup:** 11 merged findings
**Cross-referenced:** 11 verified against repo, 2 already fixed, 0 cannot verify

---

## Already Fixed

> These issues were flagged in today's reports but are no longer present in the current code.

| File | Line | Description |
|------|------|-------------|
| `lib/FocusContext.js` | L160-182 | Stale closure in timer completion callback — code already uses ref-sync pattern (`handleTimerCompleteRef`) with both synchronous and effect-based updates, preventing stale `mode` values. |
| `public/manifest.json` | — | Report B claimed "no PWA manifest at all" but `manifest.json` exists in the repo. The broader offline/service worker concern remains a feature request (issue #21). |

---

## Still Open — Carried Over

> Present in today's report AND yesterday's triage. These have persisted at least 2 days.

No carried-over issues (first run — no previous triage report found).

---

## Still Open — New Since Yesterday

> Present in today's report but NOT in yesterday's triage (first run).

| Severity | File | Line | Description | Fix Suggestion |
|----------|------|------|-------------|----------------|
| critical | `app/api/cron/cleanup/route.js` | L40-62 | Orphaned session duration inflated — crashed sessions get `ended_at = started_at + duration_minutes` instead of actual crash time, inflating time tracking stats. | Set `ended_at` to `now()` or mark session as `status: 'incomplete'` |
| critical | `app/auth/callback/route.js` | L48-55 | Open redirect via `x-forwarded-host` — user-controlled header can redirect to attacker domains after OAuth. | Whitelist allowed origins, validate `next` starts with `/`, use `NEXT_PUBLIC_APP_URL` env var |
| high | `lib/streaks.js` | L99-120 | DST boundary may break streak calculation — `setDate(getDate() - 1)` can compute wrong "yesterday" during DST transitions even though `formatDateInTimezone()` is used for string conversion. | Compute yesterday by subtracting 24h in the target timezone rather than manipulating UTC Date object |
| high | `app/api/cron/send-reminders/route.js` | L17 | Timing-attack vulnerable cron secret — all 4 cron routes use string equality (`!==`) for `CRON_SECRET` comparison. | Use `crypto.timingSafeEqual()` with Buffer conversion |
| high | `lib/FocusContext.js` | L282-294 | Context value object recreated every render — any `useFocus()` consumer re-renders on every timer tick (every second). | Memoize with `useMemo()` or split into separate TimerContext + CallbacksContext |
| high | `app/api/cron/check-streak-breaks/route.js` | L58-68 | N+1 notifications — creates one `createNotification()` call per affected user via `.map()`. Activity logs are batched but notifications are not. | Batch into single `.insert(allNotifications)` call |
| high | `lib/FocusContext.js` | L208-236 | Async IIFE in unmount cleanup runs Supabase UPDATE after component unmount — no `mountedRef` guard means rapid navigation could overwrite a freshly started session. | Add `mountedRef` flag, set to false in cleanup return, guard async write |
| medium | `app/dashboard/groups/[id]/GroupDetailClient.js` | — | Kanban board has no drag-and-drop — tasks are filtered into static columns but status changes require opening each card individually. | Integrate `@dnd-kit/core` for drag-and-drop between columns |
| medium | `lib/streaks-advanced.js` | — | No integration tests for streak freeze cooldown logic — complex cooldown (3-day waits, pool limits, timezone handling) has zero test coverage. | Add Vitest tests for `applyStreakFreeze`, `getStreakFreezeStatus`, `checkStreakAtRisk` |

---

## Cannot Verify

> Issues that could not be confirmed or denied due to missing files, vague descriptions, or out-of-range lines.

All issues were verifiable.

---

## Frontend Audit Suggestions

> Design and CSS recommendations from today's frontend audit email. Not bugs — improvement ideas.

| Component / File | Suggestion | Priority |
|-----------------|------------|----------|
| `globals.css` L307-317 | Remove global SVG noise texture overlay (`body::after`) — identified as AI-design tell | high |
| `globals.css` L288-295 | Desaturate background gradient opacities (light: 0.10->0.04, dark: 0.18->0.08) | high |
| `globals.css` L33-39 | Vary gradient angles per context instead of uniform 135deg everywhere | high |
| `globals.css` L141-147 | Tighten border-radius scale (8->6, 10->8, 14->12, 18->16px) for more professional feel | medium |
| `Sidebar.module.css` | Replace box-shadow hover states with subtle background-color change only (Arc/Linear style) | medium |
| `globals.css` L454-460 | Reduce primary button glow intensity (20px->12px, opacity 0.2->0.12) | medium |
| `globals.css` L219 | Soften dark mode border-subtle from rgba(255,255,255,0.06) to 0.04 | low |
| `globals.css` L42-49 | Desaturate primary accent from #5B5EF5 to #6366F1 to reduce visual fatigue | medium |
| `globals.css` | Neutralize background colors (#FAFAFF->#FAFAFA, #F5F4FF->#F5F5F5) to remove violet tint | medium |
| Page headers | Add letter-spacing: -0.04em to h1/h2 page titles | low |
| `components/PactCard.js` | Add colored left border (3px) by status for visual differentiation | high |
| Empty states (PactsPageClient, GroupsPageClient, DashboardClient) | Replace generic "No items yet" with branded motivational copy and illustrations | medium |
| Landing page (LandingPageClient.js) | Remove ParticleBackground, add product preview screenshot, use bento grid for features | high |
| All pages | Add motivational micro-copy and time/context-aware dashboard greetings | medium |

---

## Automated Fixes Applied

> Easy fixes that were committed in branch `fix/morning-triage-2026-03-23`.

No automated fixes applied today.

---

## Skipped — Needs Manual Review

> Issues that qualify for a fix but were skipped because they exceed the automated fix criteria.

- `app/api/cron/cleanup/route.js:L40-62` — cron route (excluded from automated fixes)
- `app/auth/callback/route.js:L48-55` — touches authentication/redirect logic (excluded)
- `lib/streaks.js:L99-120` — complex timezone/DST logic requiring careful testing (excluded)
- `app/api/cron/send-reminders/route.js:L17` (and all 4 cron routes) — cron route auth (excluded)
- `lib/FocusContext.js:L282-294` — context provider value memoization requires splitting context or careful dep array; risk of breaking consumers (excluded)
- `app/api/cron/check-streak-breaks/route.js:L58-68` — cron route (excluded)
- `lib/FocusContext.js:L208-236` — complex async unmount cleanup; risk of breaking session tracking (excluded)
- `app/dashboard/groups/[id]/GroupDetailClient.js` — feature addition (drag-and-drop), not a bug fix (excluded)
- `lib/streaks-advanced.js` — test coverage is a feature addition, not a code fix (excluded)
