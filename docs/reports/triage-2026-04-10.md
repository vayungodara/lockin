# LockIn Morning Triage — 2026-04-10

**Generated:** 2026-04-10 (automated)
**Findings:** 14 from bug-hunter + 16 from frontend-auditor = 30 total (after dedup: 24 unique)
**Cross-referenced:** 5 carried over, 5 already fixed, 14 new today
**Visual audit findings enriched:** 4 Notion rows updated to "fixed"
**Vercel health:** Landing 200, Dashboard 307 (auth redirect) — healthy

---

## Progress Since Yesterday
- Items from yesterday: 11 total, 7 fixed (in PR #59), 3 carried over, 1 new easy fix
- Commits since yesterday: 10 (PR #59 merge, review comment fixes, CLAUDE.md updates)
- Visual audit findings available: no (Chrome extension unavailable today)

## Security — RLS / Database Access
All 17 tables have RLS enabled with at least 1 policy. No security issues found.

## Already Fixed (verified in code)
| File | Line | Description |
|------|------|-------------|
| app/page.module.css | 92 + 11 others | `--font-display` replaced with `--font-body` in PR #59 |
| components/NavbarLanding.js | 19 | SSR-safe `useState(false)` — fixed in PR #59 |
| components/NavbarLanding.js | 69 | Pill navbar `role="button"` + `tabIndex` — fixed in PR #59 |
| components/LandingPageClient.js | 153, 589-590 | Hardcoded hex replaced with ACCENT_PALETTES — fixed in PR #59 |
| components/Navbar.js + ParticleBackground.js | whole files | Dead code removed — fixed in PR #59 |

## Still Open — Carried Over
| Severity | File | Line | Description | Fix Suggestion |
|----------|------|------|-------------|----------------|
| Medium | lib/partnerships.js | 241-261 | N+1 query: `notifyPartner` loops sequential DB inserts | Batch into single `.insert([...])` call |
| Medium | app/dashboard/stats/StatsPageClient.js | 44-80 | Fetches up to 5000 rows client-side for stats | Use Supabase aggregate RPCs or server-side computation |
| Low | app/dashboard/DashboardClient.js | 74, 109, 159 | `console.error` calls in client component | Replace with toast or remove (error UI already shown) |

## Still Open — New
| Severity | File | Line | Description | Fix Suggestion |
|----------|------|------|-------------|----------------|
| High | components/DailySummaryCard.js | 31 | `checkStreakAtRisk` called without timezone — uses UTC instead of user's local TZ. At-risk banner shows at wrong time. | Pass `Intl.DateTimeFormat().resolvedOptions().timeZone` as third arg |
| High | lib/streaks-advanced.js | 83 | `useStreakFreeze` export name violates ESLint hooks rule — non-hook function prefixed with `use` | Rename export to `applyStreakFreeze` or `freezeStreak`, update 7 test refs |
| Medium | app/dashboard/focus/FocusPageClient.js | 48-52 | Fetches up to 5000 focus sessions for client-side stats aggregation | Use Supabase RPC for totals, or select only needed columns |
| Medium | app/dashboard/DashboardClient.js | 105 | `localStorage.getItem` called unconditionally — not SSR-safe | Wrap in `typeof window !== 'undefined'` guard |
| Medium | app/dashboard/DashboardLayout.js | 34-38 | Timezone update fire-and-forget swallows errors silently | Add `.then(({ error }) => { if (error) console.error(...) })` |
| Medium | components/DailySummaryCard.js | 7 | Imports `CreatePactModal` — possible duplicate modal with layout-level modal | Audit to determine which should be removed |
| Low | components/JoinGroupModal.js | 9 | Imports CSS from `CreateGroupModal.module.css` instead of own file | Create dedicated file or rename to shared module |
| Low | components/FocusTimer.js | 33-37 | `defaultPalette` recomputed every render, breaks useMemo deps | **FIXED** — moved to module scope in this triage |

### Frontend-Specific — New
| Severity | File | Line | Description | Fix Suggestion |
|----------|------|------|-------------|----------------|
| Medium | app/page.module.css | 828-1037 | `.featuresDark` section has 30+ hardcoded hex values bypassing design system | Replace with `--landing-ink`, `--landing-bg`, `--landing-surface` tokens |
| Medium | app/page.module.css | 1385-1514 | CTA footer hardcodes indigo (#312E81, #1E1B4B) — breaks accent switching | Use `var(--accent-primary)` with `color-mix()` |
| Medium | components/LandingPageClient.js | multiple | 66 inline Framer Motion animations (41% of all violations) | Extract landingReveal preset, batch-convert whileInView sections |
| Medium | app/join/[code]/JoinPage.module.css | whole file | Zero dark mode overrides on user-facing invite page | Add 8-10 dark overrides matching GroupsPage pattern |
| Medium | components/Sidebar.module.css | multiple | Only 1 dark mode override — 15+ interactive elements need dark treatment | Add overrides for footer, action buttons, tooltip, mini timer |
| Medium | components/NavbarLanding.module.css | 25 | Hardcoded `rgba(244, 242, 235, 0.85)` instead of CSS variable | Define `--landing-nav-glass` token |

## Automated Fixes Applied
| File | Change | Lines Modified |
|------|--------|----------------|
| components/MonthlyCalendar.module.css:470,475 | Replaced hardcoded `#FFFFFF` with `var(--text-primary)` for dark mode calendar text | 2 |
| app/dashboard/DashboardClient.js:386 | Replaced `<a href="/dashboard/pacts">` with `<Link>` for client-side navigation | 1 |
| components/FocusTimer.js:33-37 | Moved `defaultPalette` to module scope to stabilize `useMemo` dependency | 3 |

## Skipped — Needs Manual Review
- lib/partnerships.js:241-261 — N+1 batching requires understanding notification dedup logic
- lib/streaks-advanced.js:83 — `useStreakFreeze` rename requires updating 7 test references + verifying no external consumers
- components/DailySummaryCard.js:31 — Timezone fix is straightforward but changes user-facing behavior (streak at-risk timing)
- app/page.module.css — 41 hardcoded hex values are editorial landing palette, requires design review
- components/LandingPageClient.js — 66 inline animations need systematic extraction to presets
- app/join/[code]/JoinPage.module.css — Dark mode overrides need design decisions for color choices

## DB Health Summary
- **Status:** WARNINGS (carried over)
- All 17 tables secured with RLS
- Zero orphaned rows across all 10 checks
- 1 abandoned focus session >24h (carried over)
- 4 profiles with XP drift (carried over — pre-logging XP grants)
- Row count deltas: activity_log +14, pacts +4, xp_events +4
- Full details in `docs/reports/db-health-2026-04-10.md`

## Frontend Audit Summary
- **Landing:** 6.7/10 (unchanged — 41 hardcoded hex, 66 inline animations)
- **Dashboard:** 8.6/10 (up from 8.3 — thorough dark mode recognized)
- **Pacts:** 8.4/10
- **Groups:** 8.6/10 (up from 8.1 — 15 dark mode overrides)
- **Focus:** 8.1/10
- **Stats:** 8.4/10
- **Settings:** 8.6/10 (up from 8.0 — 17 dark mode overrides)
- Full details in `docs/reports/frontend-audit-2026-04-10.md`
