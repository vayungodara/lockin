# Coverage Report — 2026-08-30

## Summary

| Metric | Before | After |
|--------|--------|-------|
| Statements | 89.56% | 94.66% |
| Branches | 77.41% | 83.42% |
| Functions | 79.41% | 97.05% |
| Lines | 92.49% | 96.18% |

Total tests: 265 → 293 (+28 new tests).

## Week-over-Week (vs 2026-06-21)

| Metric | 2026-06-21 | 2026-08-30 | Change |
|--------|-----------|-----------|--------|
| Statements | 89.56% | 94.66% | +5.10% |
| Branches | 77.41% | 83.42% | +6.01% |
| Functions | 79.41% | 97.05% | +17.64% |
| Lines | 92.49% | 96.18% | +3.69% |
| Test count | 265 | 293 | +28 |

Files that were under-covered last report and are now fully covered:
- `lib/notifications.js` — 87.27% → 100% (added `getNotifications` tests)
- `lib/activity.js` — 77.58% stmts / 29.16% funcs → 97.41% stmts / 100% funcs

Files with improved coverage:
- `lib/streaks-advanced.js` — 84.12% stmts → 94.44% stmts (added `awardStreakFreeze`, freeze status hints, RPC edge case, and error handling tests)

Files that remain under-covered across multiple weeks (highlight):
- `lib/activity.js` branches at 79.77% — remaining uncovered branches are in `getGroupStats` multi-chain error paths (lines 206-227, 235) that require per-table builder mocking for intermediate query failures
- `lib/streaks-advanced.js` lines 355-356, 377 — internal `checkStreakMilestone` notification error catch and `awardXP` marker logging failure; these are non-fatal catch blocks around dynamic imports

## Files Changed (last 7 days)

No `lib/` files were modified in the last 7 days. Fell back to lowest-coverage files overall.

## New Tests Written

- `tests/unit/lib/notifications.test.js` — 5 new tests, covers: `getNotifications` (happy path, empty result, null data, DB error, custom limit)
- `tests/unit/lib/activity.test.js` — 9 new tests, covers: `getGroupActivity` with data (profile attachment, reaction merging, fallback user info), `getAllActivity` with test data filtering (Bulk Test, Test #N, [TEST] patterns, name metadata key, limit trimming), `getGroupStats` with data (stats computation, leaderboard sorting, missing profiles)
- `tests/unit/lib/streaks-advanced.test.js` — 14 new tests, covers: `FREEZE_COOLDOWN_DAYS` export, `checkStreakAtRisk` error handling, `getStreakFreezeStatus` nextFreezeEarned hints (7/14/30-day, past all milestones, DB error), `applyStreakFreeze` RPC edge cases (success:false response, RPC throw), `awardStreakFreeze` (profile not found, already capped, RPC success, RPC failure, fallback calculation)

## Skipped (out of scope)

- `lib/FocusContext.js`, `lib/NotificationContext.js`, `lib/KeyboardShortcutsContext.js` — React context providers
- `lib/animations.js` — pure animation data presets, no logic to test
- `lib/supabase/*` — Supabase client configuration
- `lib/confetti.js` — browser-only DOM APIs, no testable pure logic
- `lib/sounds.js` — Web Audio API, browser-only
- `lib/email.js` — only export requires a real Resend client; core logic is in non-exported functions
- `lib/useKeyboardShortcuts.js`, `lib/useModalScrollLock.js` — React hooks, out of scope

## Notes

- Function coverage jumped from 79.41% to 97.05% — the biggest gain. This was driven by testing `getGroupActivity`, `getAllActivity`, `getGroupStats` (all 3 were previously at 0% function coverage despite having basic empty-data tests), and `awardStreakFreeze` (completely untested).
- `notifications.js` reached 100% across all metrics by adding the missing `getNotifications` tests.
- `activity.js` uses `mockReturnValueSequence` to test multi-query functions (`getGroupActivity`, `getAllActivity`, `getGroupStats`) that fetch from multiple tables sequentially. This avoids needing per-table builder mocking for the happy path.
- The remaining uncovered branches in `activity.js` (lines 206-227, 235) are intermediate error paths in `getGroupStats` where the second or third query fails after the first succeeds — these would need per-table mocking (like the `createTableMock` helper in streaks-advanced) to exercise.
- `streaks-advanced.js` lines 355-356 and 377 are error catches around dynamic `import()` calls (`@/lib/notifications`, `@/lib/gamification`) that fail in the test environment. These are non-fatal by design and are already handled by `vi.spyOn(console, 'error')` suppression in the existing tests.
