# LockIn Morning Triage — 2026-03-30

**Generated:** 2026-03-30 (automated)
**Findings:** 12 code findings + 9 new Cowork visual findings = 21 total
**Cross-referenced:** 8 carried over from Mar 29, 4 re-applied fixes, 0 cannot verify
**Cowork visual findings enriched:** 9 (from Notion, Mar 30 batch)
**Note:** March 29 triage branch was not merged — fixes re-applied here

---

## Progress Since Yesterday
- Items from Mar 29 triage: 7 still open (on unmerged branch), 5 fixes re-applied today
- Commits since Mar 29: 0 (Mar 29 PR not merged)
- Cowork visual findings available: yes (9 new from Mar 30, 6 carried from Mar 29)
- DB health: Supabase MCP blocked (permission error) — using Mar 28 baseline

## Vercel Health
- Vercel CLI: No VERCEL_TOKEN in environment — health check skipped
- Supabase MCP: Permission denied for execute_sql, get_project, get_logs
- Last known status (Mar 29): All deployments Ready, no 500s, production domain www.lock-in.me

---

## Already Fixed

| File | Line | Description |
|------|------|-------------|
| components/CreatePactModal.js | L148-151 | Deadline date validation — past-deadline check added in commit 4ce1940 (merged via PR #39) |

---

## Still Open — Carried Over

| Severity | File | Line | Description | Fix Suggestion |
|----------|------|------|-------------|----------------|
| high | components/ActivityFeed.js | L104-124 | `loadMore` race condition — `activities.length` used in closure creates stale offset; IntersectionObserver can fire rapid duplicate calls | Use a ref for offset, deduplicate by `id` in `setActivities`, or add AbortController |
| high | components/CreateGroupModal.js | L12-18 | `generateInviteCode` uses `Math.random()` for 6-char codes — not cryptographically secure, brutable keyspace (36^6) | Use `crypto.getRandomValues()` or increase to 8+ characters |
| high | app/dashboard/focus/FocusPageClient.js | L38-55 | "All Time" stat sums only 10 sessions (`.limit(10)` at L43) — shows wrong total. Root cause of Cowork finding LI-3 | Remove limit for stats query or run separate COUNT query |
| medium | app/dashboard/stats/StatsPageClient.js | L73-77 | Focus sessions query uses `.select('*')` with no `.limit()` — unbounded for users with many sessions | Add `.limit(500)` or select only needed fields |
| medium | components/FocusTimer.js | L71-73 | SVG gradient hardcodes `#6366F1`, `#8B5CF6`, `#D946EF` — breaks with non-indigo accent palettes | Use CSS variables via `stop-color` or SVG `currentColor` pattern |
| medium | components/LandingPageClient.js | L399 | Hardcoded hex color array `['#5B5EF5', '#7C4DFF', '#E040CB', '#00C9A7', '#FF6B6B']` for avatar dots | Extract to constant or use CSS variables |
| medium | components/CompactActivityCard.js | L138-144 | Inline Framer Motion variants instead of presets from `@/lib/animations` | Replace with `fadeInUp` or `cardHover` from animations.js |
| low | app/join/[code]/page.js | L12-37 | Error/invalid states use extensive inline `style={{...}}` instead of CSS Modules | Create shared error component or CSS module |

---

## Still Open — New

| Severity | File | Line | Description | Fix Suggestion |
|----------|------|------|-------------|----------------|
| medium | app/globals.css | L29 | Light mode `--text-muted: #A9A5BC` has ~2.7:1 contrast ratio on white — fails WCAG AA (4.5:1 required). Cowork LI-2 | Darken to ~#706C8A for 4.5:1 ratio |
| low | app/page.js | L16-18 | Logged-in users auto-redirect to /dashboard, can't view landing page. Cowork LI-25 | Add `/landing` route or query param to bypass redirect |
| low | components/MobileNav.js | L9-63 | Settings page missing from mobile navigation (only 5 items). Cowork LI-6 | Add 6th nav item for Settings or implement "More" menu |

### Cowork Visual Findings (enriched with file paths)

| ID | Severity | Finding | File | Fix |
|----|----------|---------|------|-----|
| LI-21 | medium | Dashboard dark mode: card boundaries blend into page background | app/dashboard/Dashboard.module.css | Cards use `var(--surface-1)` which is close to dark bg. Add `border: 1px solid var(--border-primary)` or subtle shadow in dark mode |
| LI-23 | low | Dashboard dark mode: 'Today' stat labels very faint | app/dashboard/Dashboard.module.css L378-380 | `.statLabel` uses `var(--text-tertiary)` (#9590AD in dark). Consider using `var(--text-secondary)` for better contrast |
| LI-24 | low | /pacts URL returns 404 — sidebar uses /dashboard/pacts | next.config.mjs | Add redirect from `/pacts` to `/dashboard/pacts` (same for /groups, /focus, /stats, /settings) |
| LI-25 | low | Landing page inaccessible when logged in | app/page.js L16-18 | Redirect blocks access. Add `/landing` route or query param bypass |
| LI-26 | medium | Gamification is purely text-based (XP/Level numbers) — no visual progression | components/XPBar.js | Feature request: add avatar, badge collection, or visual level-up animation |
| LI-27 | medium | Focus timer is plain circular ring — no ambient/immersive backgrounds | components/FocusTimer.js | Feature request: add optional ambient background themes |
| LI-28 | medium | Landing page is static — no scroll-triggered animations | components/LandingPageClient.js | Feature request: add scroll-triggered Framer Motion hero animations |
| LI-29 | medium | No streak reward escalation (7/30/100-day milestones) | lib/gamification.js | Feature request: add milestone markers with XP multipliers |
| LI-30 | medium | No social proof logo strip on landing page | components/LandingPageClient.js | Feature request: add university/institution logos or user testimonials |

---

## Automated Fixes Applied

| File | Change | Lines Modified |
|------|--------|----------------|
| components/CreateGroupModal.js | Remove unused `modalOverlay` import | 1 |
| components/CreateTaskModal.js | Remove unused `modalOverlay` import | 1 |
| app/dashboard/focus/FocusPageClient.js | Add `user.id` to `fetchSessions` useCallback dependency array | 1 |
| app/dashboard/pacts/PactsPageClient.js | Change dependency from `user` to `user.id` in fetchPacts useCallback | 1 |

---

## Skipped — Needs Manual Review

- components/ActivityFeed.js:L104 — Race condition fix requires restructuring async data flow (abort controller + dedup), exceeds 30-line easy fix scope
- components/CreateGroupModal.js:L12-18 — Invite code security fix requires crypto API + testing, touches auth-adjacent code
- app/dashboard/focus/FocusPageClient.js:L43 — "All Time" data bug requires query restructuring (separate unlimited query for total stats)
- app/globals.css:L29 — Light mode contrast fix (darken --text-muted) affects entire site, needs visual review across all pages
- components/MobileNav.js — Adding Settings to mobile nav requires UX decision (6th item vs "More" menu)
- app/dashboard/Dashboard.module.css — Dark mode card boundary fix needs testing across all 7 accent palettes
