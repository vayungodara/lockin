# Coverage Report — 2026-08-09

## Summary

| Metric | Before | After |
|--------|--------|-------|
| Statements | 89.56% | 95.25% |
| Branches | 77.41% | 84.88% |
| Functions | 79.41% | 97.05% |
| Lines | 92.49% | 96.45% |

Total tests: 265 → 300 (+35 new tests).

## Week-over-Week (vs 2026-06-21)

| Metric | 2026-06-21 | 2026-08-09 | Change |
|--------|-----------|-----------|--------|
| Statements | 89.56% | 95.25% | +5.69% |
| Branches | 77.41% | 84.88% | +7.47% |
| Functions | 79.41% | 97.05% | +17.64% |
| Lines | 92.49% | 96.45% | +3.96% |
| Test count | 265 | 300 | +35 |

Files with improved coverage:
- `lib/activity.js` — 77.58% → 99.13% statements, 29.16% → 100% functions
- `lib/notifications.js` — 87.27% → 100% statements, 83.33% → 100% functions
- `lib/streaks-advanced.js` — 84.12% → 96.82% statements, 74.11% → 90.58% branches

Files that remain at the same coverage across both reports:
- `lib/onboarding.js` — 91.07% statements
- `lib/partnerships.js` — 91.66% statements
- `lib/reactions.js` — 90.47% statements
- `lib/streaks.js` — 91.5% statements

## Files Changed (last 7 days)

No `lib/` files were modified in the last 7 days. Fell back to lowest-coverage files overall.

## New Tests Written

- `tests/unit/lib/activity.test.js` — 15 new tests, covers: `getGroupActivity` happy path (profile attachment, reactions, unknown users, deduplication), `getAllActivity` happy path (test-data filtering by title patterns, limit enforcement, null metadata handling), `getGroupStats` happy path (completion rate calculation, leaderboard building, no-activity members, activity_log/tasks error paths, unknown profile fallback)
- `tests/unit/lib/notifications.test.js` — 5 new tests, covers: `getNotifications` (success, null data, DB error, custom limit, default limit)
- `tests/unit/lib/streaks-advanced.test.js` — 15 new tests, covers: `FREEZE_COOLDOWN_DAYS` export validation, `awardStreakFreeze` (profile not found, capped at max, RPC success, RPC fallback, RPC error), `applyStreakFreeze` RPC response handling (success: false with error, default error message, RPC throw, RPC-returned freezesRemaining), `getStreakFreezeStatus` nextFreezeEarned hints (7/14/30-day suggestions, null past milestones, exception handling), `checkStreakAtRisk` error handling, `updateStreakOnCompletion` RPC failure path

## Skipped (out of scope)

- `lib/FocusContext.js`, `lib/NotificationContext.js`, `lib/KeyboardShortcutsContext.js` — React context providers
- `lib/animations.js` — pure animation data presets, no logic to test
- `lib/supabase/*` — Supabase client configuration
- `lib/confetti.js` — relies on DOM APIs (createElement, requestAnimationFrame); browser-only with no testable pure logic
- `lib/sounds.js` — relies on Web Audio API; browser-only with no testable pure logic
- `lib/email.js` — core logic is in non-exported functions; the only export requires a real Resend client
- `lib/useKeyboardShortcuts.js`, `lib/useModalScrollLock.js` — React hooks, out of scope

## Notes

- `activity.js` function coverage jumped from 29.16% to 100% by testing the happy paths of `getGroupActivity`, `getAllActivity`, and `getGroupStats`. These required mocking `getBatchReactions` from `reactions.js` (via `vi.mock`) and using `mockReturnValueSequence` for multi-query flows.
- `notifications.js` reached 100% across all metrics by adding the previously untested `getNotifications` function.
- `streaks-advanced.js` now has 96.82% statement coverage. Remaining uncovered lines (255, 327, 347, 377) are in `awardStreakFreeze`'s notification try/catch and `updateStreakOnCompletion`'s idempotency marker logging — these involve dynamic imports (`import('@/lib/notifications')`, `import('@/lib/gamification')`) that are non-fatal and guarded by try/catch.
- `activity.js` still shows 84.26% branch coverage — the uncovered branches are in `fetchProfilesMap` (the internal `profilesError` throw path) and `getAllActivity` (the `not('action', 'in', ...)` filter expression), which are difficult to exercise with the single-builder mock.
