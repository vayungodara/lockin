# Coverage Report — 2026-04-19

## Summary

| Metric | Before | After |
|--------|--------|-------|
| Statements | 79.86% | 90.31% |
| Branches | 69.44% | 78.19% |
| Functions | 67.1% | 79.2% |
| Lines | 83.33% | 93.59% |

Total tests: 187 → 261.

## Week-over-Week

- Coverage (statements): last week 74.9% → 79.86% (pre-run) → 90.31% (post-run)
- Files untested last week, now covered: `comments.js` (0% → 100%), `nudges.js` (0% → 100%), `email.js` (0% → 95.23%), `partnerships.js` (0% → 94.16%)
- Files that remain untested across multiple weeks: `lib/onboarding.js` — still at 0%, deferred due to unsupported mock chain methods (`.upsert()`). Flagged for third consecutive week.

## Files Changed (last 7 days)

- `lib/FocusContext.js` — no test written (React context, out of scope)
- `lib/confetti.js` — no test written (DOM-heavy animation utility with `requestAnimationFrame`; all exported functions return void — deferred, see Notes)
- `lib/email.js` — 0% → 95.23% statements
- `lib/gamification.js` — 98.07% → 98.07% (already well-covered, no changes needed)
- `lib/partnerships.js` — 0% → 94.16% statements
- `lib/streaks-advanced.js` — 84.12% → 88.09% statements
- `lib/supabase/middleware.js` — no test written (Supabase config, out of scope)

## New Tests Written

- `tests/unit/lib/comments.test.js` — 14 tests, covers: `getComments`, `getBatchCommentCounts`, `postComment`, `deleteComment`
- `tests/unit/lib/nudges.test.js` — 5 tests, covers: `sendNudge`
- `tests/unit/lib/email.test.js` — 6 tests, covers: `sendReminderEmail` (with Resend mock), `escapeHtml` (indirectly via HTML output), `generateReminderEmailHtml` (indirectly)
- `tests/unit/lib/partnerships.test.js` — 20 tests, covers: `sendPartnerRequest`, `acceptPartnerRequest`, `declinePartnerRequest`, `removePartnership`, `getPartnerships`, `notifyPartner`
- `tests/unit/lib/streaks.test.js` — extended with 15 new tests, now covers: `formatDateInTimezone`, `getHourInTimezone`, `getActivityHeatmap` (on top of existing `formatUTCDate`, `calculateStreak` tests)
- `tests/unit/lib/streaks-advanced.test.js` — extended with 14 new tests, now covers: `awardStreakFreeze` (direct), `FREEZE_COOLDOWN_DAYS`, `getStreakFreezeStatus.nextFreezeEarned` paths (on top of existing `checkStreakAtRisk`, `getStreakFreezeStatus`, `applyStreakFreeze`, `updateStreakOnCompletion` tests)

## Updated Test Infrastructure

- `tests/setup/supabase-mock.js` — added `.or()` and `.limit()` to chain methods, enabling tests for `partnerships.js` and `nudges.js` which were deferred last week due to missing mock support.

## Skipped (out of scope)

- `lib/FocusContext.js`, `lib/NotificationContext.js`, `lib/KeyboardShortcutsContext.js`, `lib/ThemeProvider.js` — React context providers.
- `lib/animations.js` — pure animation data presets, no logic to test.
- `lib/supabase/*` — Supabase client configuration.
- `lib/pactTemplates.js`, `lib/accentColors.js` — already at 100% coverage.
- `lib/gamification.js` — already at 98% coverage.
- `lib/confetti.js` — DOM-heavy animation utility; all 5 non-hook exports call `fireParticles` which uses `requestAnimationFrame`, `performance.now`, and returns void. Testable under happy-dom but assertions would be limited to "does not throw." Deferred.
- `lib/onboarding.js` — requires `.upsert()` mock chain method. Flagged for next week.

## Notes

- **Mock helper extended:** Added `.or()` and `.limit()` to `createMockSupabase()` chain methods. This unblocked `partnerships.js` (previously deferred for 2 weeks) and `nudges.js`.
- **Email tests:** Mocked the `resend` package constructor to test `sendReminderEmail`. The `escapeHtml` internal function gets full coverage through the HTML XSS test case.
- **`partnerships.js` UUID validation:** The `assertUuid` guard validates interpolated values before they reach Supabase's `.or()` filter string. Tests verify the guard rejects non-UUID inputs.
- **Remaining low-coverage files:** `activity.js` (77.58% statements, 29.16% functions) has untested `logActivity`, `getGroupActivity`, `getAllActivity`, `getGroupStats` functions. These require multi-table mock state; improving this is a candidate for next week's run.
- **`streaks.js` `formatLocalDate`:** Internal (not exported), called nowhere in the module. Lines 18–21 remain uncovered.
