# DB Health Check — 2026-03-27

**Status:** WARNINGS
**Run time:** 07:00 UTC
**Checks passed:** 23/24

---

## Table Sizes

| Table | Rows | Change |
|-------|------|--------|
| profiles | 15 | — |
| pacts | 21 | — |
| groups | 12 | — |
| group_members | 16 | — |
| tasks | 29 | — |
| activity_log | 293 | — |
| activity_reactions | 2 | — |
| activity_comments | 2 | — |
| focus_sessions | 52 | — |
| notifications | 8 | — |
| reminder_logs | 0 | — |
| xp_events | 52 | — |
| user_achievements | 5 | — |
| user_onboarding | 10 | — |
| accountability_partnerships | 0 | — |
| nudges | 6 | — |
| group_challenges | 0 | — |
| **Total** | **523** | **—** |

## Orphaned Rows

| Check | Count | Status |
|-------|-------|--------|
| group_members -> groups | 0 | OK |
| tasks -> groups | 0 | OK |
| activity_reactions -> activity_log | 0 | OK |
| activity_comments -> activity_log | 0 | OK |
| xp_events -> profiles | 0 | OK |
| nudges (sender) -> profiles | 0 | OK |
| nudges (receiver) -> profiles | 0 | OK |
| reminder_logs -> pacts | 0 | OK |
| partnerships (user1) -> profiles | 0 | OK |
| partnerships (user2) -> profiles | 0 | OK |

## Stale Data

| Check | Count | Status |
|-------|-------|--------|
| Abandoned focus sessions (>24h, no ended_at) | 0 | OK |
| Unread notifications >30 days | 0 | OK |
| Active pacts with deadline >7 days past | 0 | OK |
| Reminder logs >30 days | 0 | OK |
| Read notifications >60 days | 0 | OK |

## Anomalies

| Check | Count/Details | Status |
|-------|---------------|--------|
| Profiles with negative XP | 0 | OK |
| Profiles with level < 1 | 0 | OK |
| Profiles with negative streaks | 0 | OK |
| Duplicate group memberships | 0 | OK |
| Focus sessions >480 min | 0 | OK |
| Invalid recurrence_type | 0 | OK |
| current_streak > longest_streak | 0 | OK |
| XP events with xp_gained <= 0 | 0 | OK |
| XP drift (profile vs events sum) | 4 profiles | WARN |

### XP Drift Details

| Profile | Name | profile.total_xp | SUM(xp_events) | Drift |
|---------|------|-------------------|-----------------|-------|
| 4af4b971-... | Aarav Godara | 45 | 0 | +45 |
| e8c24352-... | Vayun Chintha | 45 | 0 | +45 |
| c590a081-... | LockIn Sentinel | 185 | 75 | +110 |
| 0a6eee91-... | Vayun Godara | 680 | 455 | +225 |

> All 4 profiles have `total_xp` higher than the sum of their `xp_events`. This likely means XP was awarded (e.g. via manual DB update or early code that didn't log xp_events) before the `xp_events` table was fully instrumented.

## Delta vs Yesterday

- No previous report found for comparison.

## Suggested Cleanup SQL

> **DO NOT RUN THESE.** These are suggestions for manual review only.

```sql
-- Option A: Reconcile profiles.total_xp to match xp_events sum
-- (Only run if xp_events is the source of truth)
UPDATE profiles p
SET total_xp = COALESCE(sub.computed_xp, 0)
FROM (
  SELECT user_id, SUM(xp_gained) AS computed_xp
  FROM xp_events
  GROUP BY user_id
) sub
WHERE p.id = sub.user_id
  AND p.total_xp != sub.computed_xp;

-- Option B: Backfill missing xp_events to match profiles.total_xp
-- (Only run if profiles.total_xp is the source of truth)
INSERT INTO xp_events (user_id, event_type, xp_gained, created_at)
SELECT p.id, 'legacy_backfill', p.total_xp - COALESCE(sub.computed_xp, 0), NOW()
FROM profiles p
LEFT JOIN (
  SELECT user_id, SUM(xp_gained) AS computed_xp
  FROM xp_events
  GROUP BY user_id
) sub ON sub.user_id = p.id
WHERE p.total_xp != COALESCE(sub.computed_xp, 0)
  AND p.total_xp IS NOT NULL AND p.total_xp != 0;
```

## Errors

All queries executed successfully.

---

*Generated by DB Health Check automated task.*
