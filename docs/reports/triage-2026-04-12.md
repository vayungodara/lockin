# LockIn Morning Triage — 2026-04-12

**Generated:** 2026-04-12 (automated)
**Findings:** 15 from bug-hunter + 25 from frontend-auditor = 40 total
**Cross-referenced:** First run (no yesterday baseline)
**Visual audit findings enriched:** 3 (pact contrast, streak inconsistency, calendar truncation)

---

## Progress Since Yesterday
- Items from yesterday: N/A (first triage run)
- Commits since yesterday: 1 (major dashboard redesign — 6077dd2)
- Visual audit findings available: yes (from Notion)

## Security — RLS / Database Access
All 17 tables have RLS enabled with policies. No security issues found.

| Table | RLS | Policies | Status |
|-------|-----|----------|--------|
| All 17 tables | Enabled | 1-4 each | OK |

## Already Fixed
N/A — first run.

## Still Open — New

### Critical / High

| Severity | File | Line(s) | Description | Fix Suggestion |
|----------|------|---------|-------------|----------------|
| high | `components/TodayBar.js` | 78-80 | Date boundary uses local midnight but `toISOString()` converts to UTC — pacts due today / overdue counts can be wrong for users far from UTC | Use timezone-aware date boundaries (construct start/end of day in user's timezone) |
| high | `app/dashboard/stats/StatsPageClient.js` | 42-80 | Fetches up to 5000 pacts + 5000 focus sessions, filters in-memory. Will degrade as data grows. | Use Supabase count queries with date filters server-side |

### Medium

| Severity | File | Line(s) | Description | Fix Suggestion |
|----------|------|---------|-------------|----------------|
| medium | `lib/FocusContext.js` | 334-337 | `currentWorkDuration` etc. read from refs outside `useMemo` — stale values for progress until re-render triggered | Move duration reads inside useMemo or convert to state |
| medium | `app/dashboard/DashboardClient.js` | 75-84 | `mark_overdue_pacts` RPC dedup via localStorage — shared key between Dashboard + PactsPage, fragile in incognito | Minor robustness issue. RPC is idempotent. Low priority. |
| medium | `components/NotificationBell.js` | 65-78 | 25 consecutive rAF calls to reposition dropdown — brute-force approach to handle sidebar animation | Use CSS-based positioning or ResizeObserver |

### Low

| Severity | File | Line(s) | Description | Fix Suggestion |
|----------|------|---------|-------------|----------------|
| low | `components/TodayBar.js` | 86-114 | 6 DB round-trips per pact action (4 queries + 2 streak queries refresh on every refreshKey change) | Selective refresh — only re-fetch changed data |

### Visual Audit Findings (from Notion, enriched)

| Finding | File | Details |
|---------|------|---------|
| REGRESSION: Pact card text nearly invisible in both themes | `components/PactCard.module.css` | Contrast fails WCAG AA on active/due pacts. Needs text color audit. |
| Streak count inconsistency: Dashboard vs Stats | `components/TodayBar.js` + `app/dashboard/stats/StatsPageClient.js` | Different streak calculation paths — TodayBar reads profile directly, Stats uses `calculateStreak()` |
| Stats Activity Calendar only shows first week row | `components/MonthlyCalendar.js` or `StatsPageClient.js` | Calendar grid truncated — needs investigation of row rendering logic |

## Cannot Verify
| File | Issue | Reason |
|------|-------|--------|
| `components/NotificationBell.module.css` | Missing `@keyframes bellWiggle` and `dotPulse` definitions | May be defined in globals or inherited — needs runtime check |

## Automated Fixes Applied

| File | Change | Lines Modified |
|------|--------|----------------|
| `components/TodayBar.js` | Pass timezone to `checkStreakAtRisk()` and `applyStreakFreeze()` | +4 lines |
| `components/Sidebar.js` | Add `typeof window` guard to `getSidebarCollapsed()` | +1 line |
| `app/dashboard/pacts/PactsPageClient.js` | Add `.eq('user_id', user.id)` to delete query (defense-in-depth) | +1 line |
| `components/PactCard.js` | Remove shadowed `const now = new Date()` in `formatDeadline()` | -1 line |
| `app/share/streak/page.js` | Sanitize `streak` (numeric only) and `name` (length + char limit) in OG metadata | +2 lines |
| `components/FocusTimer.js` | Move `defaultPalette` lookup inside `useMemo` to avoid per-render `.find()` | -1 line, +1 line |
| `lib/FocusContext.js` | Remove redundant `useEffect` for `handleTimerCompleteRef` sync | -3 lines |
| `components/Toast.js` | Use `crypto.randomUUID()` for toast IDs instead of `Date.now() + Math.random()` | +2 lines |

## Skipped — Needs Manual Review
- `components/TodayBar.js:78-80` — Date boundary timezone issue requires architectural decision on how to handle timezone-aware date ranges
- `app/dashboard/stats/StatsPageClient.js:42-80` — Performance optimization needs careful refactoring of data fetching strategy
- `components/PactCard.module.css` — Pact card contrast regression (visual audit finding) needs design review
- `components/MonthlyCalendar.js` — Calendar truncation needs investigation of rendering logic
