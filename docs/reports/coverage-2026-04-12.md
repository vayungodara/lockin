# Coverage Report — 2026-04-12

## Summary

| Metric | Before | After |
|--------|--------|-------|
| Statements | 32.66% | 74.9% |
| Branches | 26.66% | 67.66% |
| Functions | 28.78% | 62.5% |
| Lines | 34.41% | 78.05% |

Total tests: 104 → 183.

## Files Changed (last 7 days)

- `lib/FocusContext.js` — no test written (React context, out of scope)
- `lib/activity.js` — 16.37% → 77.58% statements
- `lib/animations.js` — no test written (animation presets, out of scope)
- `lib/notifications.js` — 0% → 87.27% statements
- `lib/onboarding.js` — no test written (Supabase chain relies on `.limit()` / `.upsert()`, not supported by the current mock helper; deferred)
- `lib/partnerships.js` — no test written (relies on `.or()` and multi-chain builder states not supported by the current mock helper; deferred)
- `lib/reactions.js` — 1.58% → 90.47% statements

## New Tests Written

- `tests/unit/lib/activity.test.js` — extended with 18 new tests, now covers: `logActivity`, `getGroupActivity`, `getAllActivity`, `getGroupStats`, `HIDDEN_FEED_ACTIONS`, `TEST_DATA_PATTERNS` (on top of the existing `formatRelativeTime` and `getActionInfo` tests).
- `tests/unit/lib/reactions.test.js` — 17 tests, covers: `REACTIONS`, `getReactions`, `getBatchReactions`, `toggleReaction`.
- `tests/unit/lib/notifications.test.js` — 22 tests, covers: `NOTIFICATION_TYPES`, `getNotificationIcon`, `getUnreadCount`, `markAsRead`, `markAllAsRead`, `createNotification`.
- `tests/unit/lib/gamification.test.js` — extended with 14 new tests, now covers: `awardXP`, `unlockAchievement`, `getUserAchievements`, `checkPactAchievements` (on top of the existing `XP_REWARDS`, `getLevelFromXP`, `getProgressToNextLevel`, `ACHIEVEMENTS` tests).
- `tests/unit/lib/accentColors.test.js` — extended with 7 new tests for `applyAccentToDocument` (happy-dom DOM manipulation).

## Skipped (out of scope)

- `lib/FocusContext.js`, `lib/NotificationContext.js`, `lib/KeyboardShortcutsContext.js`, `lib/ThemeProvider.js` — React context providers.
- `lib/animations.js` — pure animation data presets, no logic to test.
- `lib/supabase/*` — Supabase client configuration.
- `lib/pactTemplates.js` — already at 100% coverage.
- `lib/onboarding.js`, `lib/partnerships.js` — require mock helper extensions (`.limit()`, `.or()`, and per-call chain state) that would broaden the mock beyond its current contract. Noted for follow-up.
- `lib/nudges.js`, `lib/comments.js`, `lib/email.js`, `lib/confetti.js`, `lib/sounds.js`, `lib/streaks-advanced.js`, `lib/streaks.js` — not changed in the last 7 days. Some still have mid-range coverage (e.g. `streaks-advanced.js` at 60.7%, `streaks.js` at 49%); future weeks should close these gaps.

## Notes

- **First run** — no previous coverage report for comparison. Future runs should include a Week-over-Week section.
- `accentColors.js` was lifted from 15.38% to 100% because the previously untested `applyAccentToDocument` function turned out to be fully testable under happy-dom via `document.documentElement.style`.
- `reactions.toggleReaction` has a residual untested branch: the "existing query returned a non-`PGRST116` error" path would require either sequential `mockReturnValueOnce` support or per-call builder state in the mock helper; this is a candidate improvement for `tests/setup/supabase-mock.js`.
- `activity.getGroupStats` and `activity.getAllActivity` happy-path leaderboard/reactions assembly (lines 243-249 and 175-180) were not fully exercised — the shared builder mock resolves every chained `await` to the same value, which makes it hard to exercise the "group members exist + recent activity exists + profiles fetch succeeds" flow in a single test. Candidate for mock helper follow-up.
- `onboarding.js` and `partnerships.js` remained at 0% this week but are flagged for next week once the mock helper supports the chain methods they require.
