# Coverage Report — 2026-08-02

## Summary

| Metric | Before | After |
|--------|--------|-------|
| Statements | 89.56% | 94.42% |
| Branches | 77.41% | 82.87% |
| Functions | 79.41% | 97.05% |
| Lines | 92.49% | 95.9% |

Total tests: 265 → 283 (+18 new tests).

## Week-over-Week (vs 2026-06-21)

| Metric | 2026-06-21 | 2026-08-02 | Change |
|--------|-----------|-----------|--------|
| Statements | 89.56% | 94.42% | +4.86% |
| Branches | 77.41% | 82.87% | +5.46% |
| Functions | 79.41% | 97.05% | +17.64% |
| Lines | 92.49% | 95.9% | +3.41% |
| Test count | 265 | 283 | +18 |

Files that were under-covered last report and are now fully covered:
- `lib/notifications.js` — 87.27% → 100% (added getNotifications tests)

Files with improved coverage:
- `lib/activity.js` — 77.58% → 97.41% stmts, 29.16% → 100% funcs (added happy-path tests for getGroupActivity, getAllActivity, getGroupStats)
- `lib/streaks-advanced.js` — 84.12% → 92.85% stmts, 74.11% → 85.88% branches (added awardStreakFreeze tests, applyStreakFreeze RPC edge cases, getStreakFreezeStatus branch coverage)

Files that remain untested across multiple weeks (highlight):
- `lib/email.js` — core logic in non-exported functions; only export requires real Resend client
- `lib/confetti.js` — browser-only DOM APIs, no testable pure logic
- `lib/sounds.js` — browser-only Web Audio API, no testable pure logic

## Files Changed (last 7 days)

No `lib/` files were modified in the last 7 days. Fell back to lowest-coverage files overall.

## New Tests Written

- `tests/unit/lib/activity.test.js` — 4 new tests: getGroupActivity happy path with profiles + reactions, unknown profile fallback, getAllActivity test-data filtering, getGroupStats full computation with leaderboard
- `tests/unit/lib/notifications.test.js` — 3 new tests: getNotifications success, null data, DB error
- `tests/unit/lib/streaks-advanced.test.js` — 11 new tests: FREEZE_COOLDOWN_DAYS export check, awardStreakFreeze (profile not found, pool capped, RPC error, RPC success, fallback count), getStreakFreezeStatus (past all milestones, between milestones hint, exception fallback), applyStreakFreeze (RPC success=false, RPC DB error)

## Skipped (out of scope)

- `lib/FocusContext.js`, `lib/NotificationContext.js`, `lib/KeyboardShortcutsContext.js` — React context providers
- `lib/animations.js` — pure animation data presets, no logic to test
- `lib/supabase/*` — Supabase client configuration
- `lib/confetti.js` — relies on DOM APIs; browser-only with no testable pure logic
- `lib/sounds.js` — relies on Web Audio API; browser-only with no testable pure logic
- `lib/email.js` — core logic in non-exported functions; only export requires real Resend client
- `lib/useKeyboardShortcuts.js`, `lib/useModalScrollLock.js` — React hooks, out of scope

## Notes

- Function coverage jumped from 79.41% to 97.05% — the biggest gain came from exercising `fetchProfilesMap` and `isTestActivity` (internal helpers in `activity.js`) through happy-path integration tests using `mockReturnValueSequence`.
- `activity.js` branch coverage (76.4%) remains lower than statement coverage because Supabase chain methods (`.eq()`, `.order()`, `.range()`) generate implicit branches in v8 coverage that are artifacts of the chaining pattern, not real logic branches.
- `streaks-advanced.js` uncovered lines 355-356 and 377 are inside `try/catch` blocks around dynamic `import()` calls for notification sending and XP marker logging — these paths are non-fatal (the functions succeed regardless) and would require module-level mocking to exercise.
- `email.js` would benefit from refactoring `escapeHtml` and `generateReminderEmailHtml` to be exported, which would make them directly testable without mocking Resend.
