# LockIn DB Health — 2026-03-31

**Generated:** 2026-03-31 (automated)
**Status:** UNABLE TO RUN — SUPABASE MCP UNAVAILABLE

---

## Error

The Supabase MCP server returned permission errors (`MCP error -32600: You do not have permission to perform this action`) for all operations including `execute_sql`, `list_tables`, `get_project`, and `get_advisors`. This is a recurring issue also observed on 2026-03-29.

Both the orchestrator and the `@db-health-checker` subagent failed to connect.

**Action required:** Verify Supabase MCP credentials and permissions are correctly configured.

---

## Previous Baseline (2026-03-28) for Reference

### Table Sizes (as of 2026-03-28)

| Table | Row Count |
|-------|-----------|
| activity_log | 294 |
| focus_sessions | 52 |
| xp_events | 53 |
| tasks | 29 |
| pacts | 21 |
| group_members | 16 |
| profiles | 15 |
| groups | 12 |
| user_onboarding | 10 |
| notifications | 8 |
| nudges | 6 |
| user_achievements | 5 |
| activity_reactions | 2 |
| activity_comments | 2 |
| reminder_logs | 0 |
| accountability_partnerships | 0 |
| group_challenges | 0 |
| **Total** | **525** |

### Previous Orphaned Rows — All OK
### Previous Stale Data — All OK

### Previous Anomalies

| Check | Count | Status |
|-------|-------|--------|
| XP drift | 4 profiles | WARN |

**XP Drift Details (2026-03-28):**

| Profile | full_name | profile_xp | computed_xp | drift |
|---------|-----------|------------|-------------|-------|
| 4af4b971-... | Aarav Godara | 45 | 0 | +45 |
| e8c24352-... | Vayun Chintha | 45 | 0 | +45 |
| c590a081-... | LockIn Sentinel | 185 | 75 | +110 |
| 0a6eee91-... | Vayun Godara | 690 | 465 | +225 |

> All 4 profiles have more XP in their profile than the sum of xp_events records. XP was likely awarded through a path that doesn't log to xp_events.

### Outstanding Security Findings (from 2026-03-29, unverified)

| Issue | Severity |
|-------|----------|
| `get_user_group_ids(uid)` function granted to PUBLIC with no auth check | CRITICAL |
| 8 SECURITY DEFINER functions granted to PUBLIC | HIGH |
| 8 tables with authenticated DELETE grants but no DELETE policies | MEDIUM |

---

## Suggested Cleanup SQL

> **DO NOT RUN THESE.** For manual review only. Carried over from previous report.

```sql
-- Investigate XP drift
SELECT p.id, p.full_name, p.total_xp,
       COALESCE(SUM(xe.xp_gained), 0) AS events_total,
       p.total_xp - COALESCE(SUM(xe.xp_gained), 0) AS drift
FROM profiles p
LEFT JOIN xp_events xe ON xe.user_id = p.id
GROUP BY p.id, p.full_name, p.total_xp
HAVING p.total_xp != COALESCE(SUM(xe.xp_gained), 0)
   AND p.total_xp IS NOT NULL AND p.total_xp != 0
ORDER BY drift DESC;
```

---

## Errors

| Check | Error |
|-------|-------|
| ALL | Supabase MCP returned `MCP error -32600: You do not have permission to perform this action` for all queries |

---

*Generated by Morning Triage automated task.*
