# Coverage Report — 2026-04-05

## Week-over-Week

First run — no previous report for comparison.

## Summary

| Metric | Before | After |
|--------|--------|-------|
| Statements | 32.66% | 49.83% |
| Branches | 26.66% | 42.09% |
| Functions | 28.78% | 52.00% |
| Lines | 34.41% | 51.92% |

## Files Changed (last 7 days)

- `lib/activity.js` — 16.37% -> 31.03% statements
- `lib/animations.js` — no test written (animation presets, out of scope)
- `lib/gamification.js` — 10% -> 48% statements
- `lib/partnerships.js` — 0% -> 52.29% statements (new test file)
- `lib/supabase/middleware.js` — no test written (Supabase config, out of scope)

## New Tests Written

- `tests/unit/lib/reactions.test.js` — 16 tests, covers: REACTIONS, getReactions, getBatchReactions, toggleReaction
- `tests/unit/lib/partnerships.test.js` — 12 tests, covers: sendPartnerRequest, acceptPartnerRequest, declinePartnerRequest, removePartnership, getPartnerships

## Expanded Tests

- `tests/unit/lib/activity.test.js` — added 15 tests for: logActivity (success, auth failure, insert error, non-object metadata), HIDDEN_FEED_ACTIONS, TEST_DATA_PATTERNS
- `tests/unit/lib/gamification.test.js` — added 8 tests for: awardXP (success, rpc error, metadata passing), getUserAchievements (success, error, empty), onboarding_complete achievement

## Infrastructure

- `tests/setup/supabase-mock.js` — added `.or()` to chainable builder methods

## Skipped (out of scope)

- `lib/animations.js` — pure animation preset data, no logic to test
- `lib/supabase/middleware.js` — Supabase middleware config
- `lib/accentColors.js` — `applyAccentToDocument` is DOM-dependent; `hexToRgb` and palette data already well-covered
- `lib/FocusContext.js`, `lib/NotificationContext.js`, `lib/KeyboardShortcutsContext.js` — React context providers

## Notes

- Functions involving multiple sequential Supabase queries (e.g. `getGroupActivity`, `getAllActivity`, `getGroupStats`, `unlockAchievement`, `checkPactAchievements`) are difficult to fully test with the current single-builder mock pattern since all `.from()` calls share the same mock response. These would benefit from integration tests or a more sophisticated per-table mock in the future.
- `partnerships.js` functions that use dynamic `import('@/lib/notifications')` required `vi.mock` to avoid import errors in the test environment.
- Overall test count went from 104 to 147 (+43 tests).
