# Coverage Report — 2026-08-16

## Summary

| Metric | Before | After |
|--------|--------|-------|
| Statements | 89.56% | 94.18% |
| Branches | 77.41% | 82.33% |
| Functions | 79.41% | 97.05% |
| Lines | 92.49% | 95.63% |

Total tests: 265 → 291 (+26 new tests).

## Week-over-Week (vs 2026-06-21)

| Metric | 2026-06-21 | 2026-08-16 | Change |
|--------|-----------|-----------|--------|
| Statements | 89.56% | 94.18% | +4.62% |
| Branches | 77.41% | 82.33% | +4.92% |
| Functions | 79.41% | 97.05% | +17.64% |
| Lines | 92.49% | 95.63% | +3.14% |
| Test count | 265 | 291 | +26 |

Files that were under-covered last report and are now fully covered:
- `lib/notifications.js` — 87.27% → 100% (added `getNotifications` tests)
- `lib/activity.js` — 77.58% stmts / 29.16% funcs → 97.41% stmts / 100% funcs (added data-path tests for `getGroupActivity`, `getAllActivity`, `getGroupStats`)

Files with improved coverage:
- `lib/streaks-advanced.js` — 84.12% → 91.26% (added `awardStreakFreeze` edge cases, `updateStreakOnCompletion` error path, `getStreakFreezeStatus` milestone hints, `checkStreakAtRisk` error handling)

## Files Changed (last 7 days)

No `lib/` files were modified in the last 7 days. Fell back to lowest-coverage files overall.

## New Tests Written

- `tests/unit/lib/notifications.test.js` — 6 new tests, covers: `getNotifications` (happy path, empty result, null data, DB error, limit parameter, default limit)
- `tests/unit/lib/activity.test.js` — 9 new tests, covers: `getGroupActivity` (profile/reaction attachment, unknown user fallback), `getAllActivity` (profile/reaction enrichment, test-data filtering, limit trimming), `getGroupStats` (full stats + leaderboard computation, sort order, unknown profile fallback, 100% completion)
- `tests/unit/lib/streaks-advanced.test.js` — 11 new tests, covers: `awardStreakFreeze` (profile not found, max cap, RPC failure), `updateStreakOnCompletion` (RPC error, idempotency lookup failure), `checkStreakAtRisk` (DB error), `getStreakFreezeStatus` (nextFreezeEarned hints for each milestone tier, past all milestones, DB error)

## Skipped (out of scope)

- `lib/FocusContext.js`, `lib/NotificationContext.js`, `lib/KeyboardShortcutsContext.js` — React context providers
- `lib/animations.js` — pure animation data presets, no logic to test
- `lib/supabase/*` — Supabase client configuration
- `lib/confetti.js` — relies on DOM APIs; browser-only with no testable pure logic
- `lib/sounds.js` — relies on Web Audio API; browser-only with no testable pure logic
- `lib/email.js` — only export (`sendReminderEmail`) requires a real Resend client; core logic in non-exported functions
- `lib/useKeyboardShortcuts.js`, `lib/useModalScrollLock.js` — React hooks, out of scope

## Notes

- The largest improvement was in function coverage (79.41% → 97.05%), primarily from exercising `getGroupActivity`, `getAllActivity`, and `getGroupStats` with actual data flowing through `fetchProfilesMap` (internal) and `isTestActivity` (internal).
- `notifications.js` reached 100% across all metrics — `getNotifications` was the only untested export.
- `activity.js` branch coverage remains at 77.52% — the uncovered branches are inside `fetchProfilesMap` (the `throw profilesError` path at line 29) and `getGroupStats` (error paths from intermediate queries at lines 206-227, 235). These require per-table mocking similar to `createTableMock()` in `streaks-advanced.test.js` and are candidates for a future session.
- `streaks-advanced.js` lines 347 and 377 remain uncovered — they are inside dynamic `import()` calls to `@/lib/notifications` and `@/lib/gamification` within `awardStreakFreeze` and `checkStreakMilestone`. These internal functions succeed or silently catch in the test environment; full coverage would require module-level mocking of the dynamic imports.
