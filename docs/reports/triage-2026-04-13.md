# LockIn Morning Triage — 2026-04-13

**Generated:** 2026-04-13 (automated)
**Findings:** 18 from bug-hunter + frontend-auditor findings = 18+ total
**Cross-referenced:** 3 carried over from Apr 9, 2 fixed since Apr 9, 13 new
**Visual audit findings enriched:** 1 (dark mode card separation — matched Notion finding)
**Vercel health:** Landing 200, Dashboard 307 (auth redirect) — healthy

---

## Progress Since Yesterday
- Last triage: 2026-04-09 (gap due to dashboard redesign work Apr 10-12)
- Commits since 24h ago: 0 (stable post-redesign)
- Visual audit findings available: yes (Notion entries, but Chrome browsing unavailable since Apr 10)

## Security — RLS / Database Access
All 17 tables have RLS enabled with at least 1 policy. No security issues found.

## Already Fixed (since Apr 9 triage)
| File | Line | Description |
|------|------|-------------|
| app/globals.css | 103 | `--font-display` now defined as `'Instrument Sans', var(--font-sans)` — was CRITICAL in Apr 9 triage |
| components/NavbarLanding.js | — | SSR hydration fix, a11y pill button, unused import removal — all applied in Apr 9 |
| components/LandingPageClient.js | — | Hardcoded hex colors replaced with ACCENT_PALETTES — applied in Apr 9 |
| components/Navbar.js + ParticleBackground.js | — | Dead code removed in Apr 9 |

## Still Open — Carried Over from Apr 9
| Severity | File | Line | Description | Fix Suggestion |
|----------|------|------|-------------|----------------|
| Medium | lib/partnerships.js | 241-261 | N+1 query: `notifyPartner` loops sequential inserts | Batch into single `.insert([...])` call |
| Low | app/api/cron/send-reminders/route.js | 42-74 | Sequential email sends + DB inserts in loop | Expected for email rate limits; batch reminder_logs only |
| Low | app/dashboard/stats/StatsPageClient.js | 44-80 | Fetches up to 5000 rows client-side for stats | Consider server-side aggregation RPC |

## Still Open — New
| Severity | File | Line | Description | Fix Suggestion |
|----------|------|------|-------------|----------------|
| High | next.config.mjs | 49-63 | No Content-Security-Policy header | Add CSP header with `'self'`, `'unsafe-inline'` for scripts/styles, allowed image sources |
| High | lib/partnerships.js | 19+ | UUID string interpolation in `.or()` filters — filter injection risk if untrusted input | Validate UUID format before interpolation |
| Medium | lib/activity.js | 146-188 | `getAllActivity` has no explicit user/group scope, relies entirely on RLS | Verify RLS policy restricts SELECT to user's groups |
| Medium | app/share/streak/page.js | 6-18 | ~~XSS via unsanitized searchParams in metadata~~ | **FIXED** — sanitized name + parsed streak as integer |
| Medium | components/ActivityFeed.js | 136-145 | ~~Duplicate activities on "load more" with realtime inserts~~ | **FIXED** — added dedup in loadMore |
| Medium | components/PactCard.js | 195-199 | Timezone-dependent "due today" uses `toDateString()` comparison | Use explicit todayStart/todayEnd range check |
| Medium | lib/streaks-advanced.js | 298-301 | Streak freeze awarded on every completion at milestone, not once | Track awarded milestones to prevent duplicates |
| Medium | app/dashboard/DashboardClient.js | 86-91 | Fetches ALL pacts (200 limit) but only shows 3 on dashboard | Reduce limit or add targeted query |
| Medium | components/CreateGroupModal.js | 167 | ~~No maxLength on group name input~~ | **FIXED** — added `maxLength={100}` |
| Low | components/Sidebar.js | 25-28 | `getSidebarCollapsed` accesses localStorage without window guard | Add `typeof window === 'undefined'` guard |
| Low | lib/FocusContext.js | 191 | `sessionsCompleted` stale closure in `handleTimerComplete` | Use functional setState updater |
| Low | components/CreateTaskModal.js | 150 | ~~Missing aria-label for task title input~~ | **FIXED** — added `aria-label="Task title"` |
| Low | app/share/streak/page.js | 21 | ~~Unused `searchParams` prop on page component~~ | **FIXED** — removed unused param |
| Low | components/FocusTimer.js | 32 | ~~`defaultPalette` computed every render~~ | **FIXED** — moved to module scope |
| Low | lib/email.js | 19 | ~~`toLocaleString` uses server timezone (UTC) without label~~ | **FIXED** — added `timeZone: 'UTC'` + "(UTC)" suffix |
| Low | components/NotificationBell.js | 66-78 | 25 rAF calls on each dropdown open for position tracking | Replace with single position calc + resize listener |
| Low | components/CreateGroupModal.js | 12-20 | Modular bias in invite code generation (256 % 36) | Use rejection sampling |

## Automated Fixes Applied
| File | Change | Lines Modified |
|------|--------|----------------|
| app/share/streak/page.js:8-9 | Sanitized searchParams: parseInt streak, strip `<>"` from name, limit 50 chars | 2 |
| app/share/streak/page.js:21 | Removed unused `searchParams` prop from page component | 1 |
| components/ActivityFeed.js:138 | Added dedup filter in loadMore to prevent duplicate activities from realtime race | 4 |
| components/CreateGroupModal.js:162 | Added `maxLength={100}` to group name input | 1 |
| components/CreateTaskModal.js:146 | Added `aria-label="Task title"` for screen reader accessibility | 1 |
| lib/email.js:19 | Added `timeZone: 'UTC'` to toLocaleString + "(UTC)" suffix for clarity | 2 |
| components/FocusTimer.js:32 | Moved `defaultPalette` ACCENT_PALETTES.find to module scope (avoids per-render recomputation) | 3 |

## Skipped — Needs Manual Review
- next.config.mjs — CSP header needs careful crafting to not break inline scripts/styles
- lib/partnerships.js — UUID validation and N+1 batching require understanding notification dedup logic
- lib/activity.js — RLS policy verification requires DB admin inspection
- lib/streaks-advanced.js — Streak freeze milestone tracking requires schema change
- app/dashboard/DashboardClient.js — Pacts query limit reduction needs verification that no other code uses full array
- components/PactCard.js — Timezone behavior may be intentionally browser-local
- components/NotificationBell.js — rAF position tracking is architectural, not a quick fix

## DB Health Summary
- **Status:** WARNINGS (carried over)
- All 17 tables secured with RLS (all policies intact)
- Zero orphaned rows across all 10 checks
- 1 abandoned focus session (>24h) — carried over
- 4 profiles with XP drift (stable, pre-logging grants)
- activity_log +27 rows, pacts +4, xp_events +12 since Apr 9
- Full details in `docs/reports/db-health-2026-04-13.md`
