# LockIn Morning Triage — 2026-03-28

**Generated:** 2026-03-28 08:00 CET (automated)
**Sources:** Morning Report A: missing, Morning Report B: present, Frontend Audit: missing
**Findings before dedup:** [0 from A] + [3 from B] = 3 total before merge
**After dedup:** 3 merged findings
**Cross-referenced:** 3 verified against repo, 1 already fixed, 0 cannot verify

---

## Already Fixed

> These issues were flagged in today's reports but are no longer present in the current code.

| File | Line | Description |
|------|------|-------------|
| supabase/checkpoint8_complete.sql | L23 | Monthly recurrence type constraint — migration `add_monthly_recurrence.sql` was applied; DB constraint now includes `'monthly'`. |

---

## Still Open — Carried Over

> Present in today's report AND yesterday's triage. These have persisted at least 2 days.

No carried-over issues. (First run — no previous triage report found.)

---

## Still Open — New Since Yesterday

> Present in today's report but NOT in yesterday's triage (or first run).

| Severity | File | Line | Description | Fix Suggestion |
|----------|------|------|-------------|----------------|
| medium | components/CreatePactModal.js | L146 | Deadline date not validated against current date/time. Client-side `min={today}` on the date input can be bypassed; no programmatic check before DB insert. Users could create pacts with past deadlines, breaking overdue logic. | Add a check before insert: `if (deadlineDateTime < new Date()) { setError('Deadline must be in the future'); return; }` |
| medium | components/ActivityFeed.js | L46 | `loadActivities` is called when `groupId` changes but pending requests are not cancelled. Rapidly switching groups can cause stale responses to overwrite newer data (race condition). | Add an abort controller or a stale-request ref flag in the useEffect cleanup to ignore outdated responses. |

---

## Cannot Verify

> Issues that could not be confirmed or denied due to missing files, vague descriptions, or out-of-range lines.

All issues were verifiable.

---

## Frontend Audit Suggestions

> Design and CSS recommendations from today's frontend audit email. Not bugs — improvement ideas.

Frontend audit email had not arrived at triage time.

---

## Automated Fixes Applied

> Easy fixes that were committed in branch `fix/morning-triage-2026-03-28`.

| File | Change | Lines Modified |
|------|--------|----------------|
| components/CreatePactModal.js | Add past-deadline validation check before DB insert in handleSubmit | 5 |

---

## Skipped — Needs Manual Review

> Issues that qualify for a fix but were skipped because they exceed the automated fix criteria.

- components/ActivityFeed.js:L46 — Race condition fix requires restructuring the useEffect/useCallback pattern for `loadActivities`; touches async data flow across multiple functions (>30 lines of conceptual change, needs architectural decision on abort controller vs stale ref approach).

> Note: Morning Report A had not arrived at triage time. That section's findings will be empty.
