# Coverage Report — 2026-06-21

## Summary

| Metric | Before | After |
|--------|--------|-------|
| Statements | 79.86% | 89.56% |
| Branches | 69.44% | 77.41% |
| Functions | 67.1% | 79.41% |
| Lines | 83.33% | 92.49% |

Total tests: 187 → 265 (+78 new tests).

## Week-over-Week (vs 2026-04-12)

| Metric | 2026-04-12 | 2026-06-21 | Change |
|--------|-----------|-----------|--------|
| Statements | 74.9% | 89.56% | +14.66% |
| Branches | 67.66% | 77.41% | +9.75% |
| Functions | 62.5% | 79.41% | +16.91% |
| Lines | 78.05% | 92.49% | +14.44% |
| Test count | 183 | 265 | +82 |

Files that were untested last report and are now covered:
- `lib/comments.js` — 0% → 100%
- `lib/nudges.js` — 0% → 100%
- `lib/cronAuth.js` — 0% → 92.3%
- `lib/onboarding.js` — 0% → 91.07%
- `lib/partnerships.js` — 0% → 91.66%

Files with improved coverage:
- `lib/streaks.js` — 49.05% → 91.5% (added formatDateInTimezone, getHourInTimezone, getActivityHeatmap tests)

## Files Changed (last 7 days)

No `lib/` files were modified in the last 7 days. Fell back to lowest-coverage files overall.

## New Tests Written

- `tests/unit/lib/comments.test.js` — 14 tests, covers: getComments, getBatchCommentCounts, postComment, deleteComment
- `tests/unit/lib/nudges.test.js` — 4 tests, covers: sendNudge (auth, rate-limit, happy path, DB error)
- `tests/unit/lib/cronAuth.test.js` — 6 tests, covers: verifyCronSecret (valid token, wrong token, missing header, missing env, length mismatch, no Bearer prefix)
- `tests/unit/lib/partnerships.test.js` — 23 tests, covers: sendPartnerRequest, acceptPartnerRequest, declinePartnerRequest, removePartnership, getPartnerships, notifyPartner
- `tests/unit/lib/onboarding.test.js` — 13 tests, covers: getOnboardingState, detectProgress, syncProgress, resetOnboarding
- `tests/unit/lib/streaks.test.js` — extended with 18 new tests for: formatDateInTimezone, getHourInTimezone, getActivityHeatmap, plus additional calculateStreak edge cases

## Infrastructure Changes

- `tests/setup/supabase-mock.js` — added `or`, `limit`, and `upsert` to chainable builder methods, enabling tests for `partnerships.js` and `onboarding.js` which were previously deferred

## Skipped (out of scope)

- `lib/FocusContext.js`, `lib/NotificationContext.js`, `lib/KeyboardShortcutsContext.js` — React context providers
- `lib/animations.js` — pure animation data presets, no logic to test
- `lib/supabase/*` — Supabase client configuration
- `lib/confetti.js` — relies on DOM APIs (createElement, requestAnimationFrame, performance.now) and React hooks (useCallback); browser-only with no testable pure logic
- `lib/sounds.js` — relies on Web Audio API (AudioContext); browser-only with no testable pure logic
- `lib/email.js` — core logic is in non-exported functions (escapeHtml, generateReminderEmailHtml); the only export (sendReminderEmail) requires a real Resend client
- `lib/useKeyboardShortcuts.js`, `lib/useModalScrollLock.js` — React hooks, out of scope

## Notes

- `partnerships.js` and `onboarding.js` were flagged as deferred in the 2026-04-12 report due to missing mock chain methods (`.or()`, `.limit()`, `.upsert()`). These are now supported and both files have >90% statement coverage.
- `cronAuth.js` uses `crypto.timingSafeEqual` which works in the Node.js test environment. One branch (the `timingSafeEqual` false-return path at line 36) is not directly covered because the length-check branch catches most mismatch cases first; achieving the same-length-different-content case requires a specific secret length match.
- `email.js` would benefit from refactoring `escapeHtml` and `generateReminderEmailHtml` to be exported, which would make them directly testable without mocking Resend.
- `activity.js` remains at 77.58% statements — the uncovered lines are in `getGroupStats` and `getAllActivity` which require multi-chain builder states to fully exercise.
