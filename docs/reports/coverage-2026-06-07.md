# Coverage Report — 2026-06-07

## Summary

| Metric | Before | After |
|--------|--------|-------|
| Statements | 79.86% | 90.12% |
| Branches | 69.44% | 78.55% |
| Functions | 67.1% | 79.2% |
| Lines | 83.33% | 92.91% |

Total tests: 187 → 275 (+88 new tests).

## Week-over-Week

Coverage: 74.9% → 90.12% statements (vs 2026-04-12 report).

Files that were untested last week and are now covered:
- `lib/comments.js` — 0% → 96.36% statements
- `lib/nudges.js` — 0% → 100% statements
- `lib/onboarding.js` — 0% → 92.85% statements
- `lib/partnerships.js` — 0% → 95% statements

Files that remain untested across multiple weeks (highlighted):
- `lib/email.js` — 0% (requires Resend client mock; deferred)
- `lib/confetti.js` — 0% (React hook + DOM animation; out of scope)
- `lib/sounds.js` — 0% (Web Audio API; out of scope)

Previously mid-range files improved:
- `lib/streaks.js` — 49.05% → 93.39% statements

## Files Changed (last 7 days)

No `lib/` files were modified in the last 7 days. Fell back to lowest-coverage files.

## New Tests Written

- `tests/unit/lib/comments.test.js` — 16 tests, covers: getComments, getBatchCommentCounts, postComment, deleteComment
- `tests/unit/lib/nudges.test.js` — 6 tests, covers: sendNudge (auth, rate limit, success, insert error, network error, null profile)
- `tests/unit/lib/onboarding.test.js` — 18 tests, covers: getOnboardingState, detectProgress, syncProgress, resetOnboarding
- `tests/unit/lib/partnerships.test.js` — 27 tests, covers: sendPartnerRequest, acceptPartnerRequest, declinePartnerRequest, removePartnership, getPartnerships, notifyPartner
- `tests/unit/lib/streaks.test.js` — 21 additional tests (extended), covers: formatDateInTimezone, getHourInTimezone, calculateStreak (gap/yesterday/dedup), getActivityHeatmap

## Skipped (out of scope)

- `lib/FocusContext.js`, `lib/NotificationContext.js`, `lib/KeyboardShortcutsContext.js`, `lib/ThemeProvider.js` — React context providers
- `lib/animations.js` — pure animation data presets, no logic to test
- `lib/supabase/*` — Supabase client configuration
- `lib/confetti.js` — React hook (`useConfetti`) + DOM particle animation; requires browser APIs not available in happy-dom
- `lib/sounds.js` — Web Audio API (`AudioContext`); not available in happy-dom
- `lib/email.js` — only export (`sendReminderEmail`) requires a Resend client; internal helpers (`escapeHtml`, `generateReminderEmailHtml`) are not exported

## Notes

- Extended `tests/setup/supabase-mock.js` with additional chain methods (`or`, `limit`, `upsert`, `gt`, `lt`, `lte`, `like`, `ilike`, `is`) to support testing files that use these Supabase query methods.
- `partnerships.js` requires valid UUID strings due to internal `assertUuid()` validation — tests use proper UUID format (`00000000-0000-0000-0000-000000000001`).
- `onboarding.test.js` uses a local `createTableMock` helper (same pattern as `streaks-advanced.test.js`) for `detectProgress` which queries 4 different tables in `Promise.all`.
- Remaining uncovered lines are mostly notification side-effects in catch blocks or edge cases that require per-call builder state differentiation (e.g. `comments.js:99-100` — notification to activity author when commenter differs from author).
- `lib/email.js` could be tested by mocking the `Resend` module import, but that would require `vi.mock('resend')` which is better suited to an integration test strategy.
