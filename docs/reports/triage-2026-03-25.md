# LockIn Morning Triage — 2026-03-25

**Generated:** 2026-03-25 08:00 CET (automated)
**Sources:** Morning Report A: present, Morning Report B: present, Frontend Audit: present
**Findings before dedup:** 3 from A + 3 from B = 6 total
**After dedup:** 6 merged findings
**Cross-referenced:** 6 verified against repo, 3 already fixed, 0 cannot verify

---

## Already Fixed

> These issues were flagged in today's reports but are no longer present in the current code.

| File | Line | Description |
|------|------|-------------|
| lib/partnerships.js | L41-46, L88-93 | Unchecked `.single()` on profile fetches — code already uses `profile?.full_name \|\| 'Someone'` fallback at both call sites (L54, L101). |
| components/ActivityFeed.js | L112-118 | useEffect cleanup allegedly missing dependency array — the cleanup effect already has `}, []);` (empty deps), runs only on unmount. |
| lib/FocusContext.js | L183-186 | `handleTimerCompleteRef` sync assignment on every render — this is an intentional pattern (documented in comments L180-182) to prevent stale closures in the interval. The useEffect at L184-186 has `[handleTimerComplete]` deps. |

---

## Still Open — Carried Over

> Present in today's report AND yesterday's triage. These have persisted at least 2 days.

No carried-over issues (first run).

---

## Still Open — New Since Yesterday

> Present in today's report but NOT in yesterday's triage (or first run).

| Severity | File | Line | Description | Fix Suggestion |
|----------|------|------|-------------|----------------|
| low | components/ActivityItem.js | L56 | Dynamic CSS class construction from `actionInfo.color` — builds class name via string interpolation (`styles[\`icon${color}\`]`). If color is unexpected, `styles[undefined]` silently produces `undefined` in class string. Not a security risk (CSS Modules scoped) but fragile. | Use a lookup map of color→class instead of dynamic string construction. |
| low | lib/gamification.js | L147-154 | `createNotification()` return value not checked in achievement unlock flow. If notification insert fails silently, no error is logged. Inconsistent with partnerships.js which checks the result. | Add `const notifResult = await createNotification(...)` and check `notifResult.success`. **Applied as automated fix.** |
| low | app/dashboard/groups/[id]/GroupDetailClient.js | L119-142 | Owner profile fetch runs as a second sequential query after member profile fetch. Could be merged into one batch by collecting all user IDs (members + task owners) upfront. Currently at most 2 round trips, not N+1. | Merge `memberUserIds` and task `owner_id` values into a single `.in()` query before the focus sessions fetch. |

---

## Cannot Verify

> Issues that could not be confirmed or denied due to missing files, vague descriptions, or out-of-range lines.

All issues were verifiable.

---

## Frontend Audit Suggestions

> Design and CSS recommendations from today's frontend audit email. Not bugs — improvement ideas.
> **Frontend is the developer's favorite area — go in depth here.**

| Component / File | Suggestion | Priority | Current Code | Recommended Change |
|-----------------|------------|----------|--------------|-------------------|
| **components/LandingPageClient.js:345 + app/page.module.css:135** | **Replace screenshot placeholder with real dashboard image (DAY 2)** | **high** | `<div className={styles.screenshotPlaceholder}>` renders a 400px gray box with "Your dashboard, visualized" text. CSS: `.screenshotPlaceholder { height: 400px; background: var(--bg-secondary); }` | Take a dark-mode dashboard screenshot at 1800x1000px. Save as `/public/dashboard-preview.png`. Replace the placeholder div with: `<Image src="/dashboard-preview.png" alt="LockIn Dashboard" width={1800} height={1000} priority className={styles.screenshotImage} />` wrapped in a div with `border-radius: var(--radius-lg); overflow: hidden;`. This is the #1 credibility gap — every visitor sees it. |
| **app/dashboard/DashboardClient.js** | **Promote streak counter to dashboard hero position (DAY 2)** | **high** | Streak is nested inside `DailySummaryCard` — not the first thing users see. The streak card CSS was fixed yesterday (hardcoded rgba → `var(--streak-color-rgb)`) but its position hasn't changed. | Extract streak into a standalone `StreakHero` component. Position as first element in dashboard grid, full-width. Use `var(--text-4xl)` for the count, fire emoji, `streakCelebration` animation on milestone days (7, 14, 30, 100). Background: `var(--gradient-subtle)` with `rgba(var(--streak-color-rgb), 0.06)`. Add new CSS var `--shadow-streak: 0 0 30px rgba(var(--streak-color-rgb), 0.2)` to globals.css. Micro-copy: "Don't break the chain." Reference: Duolingo's home screen leads with streak. |
| **app/page.module.css:628** | **Stagger "How It Works" card heights (DAY 2 partial)** | **high** | All `.step` cards at same Y position: `max-width: 320px`, `padding: var(--space-10) var(--space-8)`, `border-radius: var(--radius-2xl)`. Connecting lines were added but cards remain uniform. | Add vertical offsets for visual rhythm: `.step:nth-child(2) { transform: translateY(-12px); }` `.step:nth-child(3) { transform: translateY(8px); }`. Combine with existing hover transform. This creates an organic, human-designed feel vs. template uniformity. Reference: Sunsama's staggered feature sections. |
| **components/LandingPageClient.js** | **Add social proof section to landing page** | **high** | No social proof anywhere on landing — hero → features → app preview → how it works → CTA → footer. Zero testimonials, user count, or university logos. | Add between features and app preview: `background: var(--surface-2); padding: var(--space-8) var(--space-6); text-align: center;`. Content: avatar stack (3-5 user avatars using `var(--radius-full)` border-radius, overlapping with `margin-left: -8px`) + "Join 50+ students building better habits" in `var(--text-tertiary)`. Even mock data beats nothing. Use `fadeInUp` animation from `@/lib/animations`. |
| **components/LandingPageClient.js + app/page.module.css:323** | **Make feature hero card content-driven, not position-driven** | **medium** | Feature grid uses bookend layout: `index === 0 \|\| index === features.length - 1` determines large cards. Position-based, not content-based. | Add `isHero: true` flag to the Focus Timer feature object (the most visual feature). Only `isHero` features get `styles.featureCardLarge`. Add an inline SVG timer ring inside the hero card using `stroke: var(--accent-primary)`. Content-driven sizing beats algorithmic positioning. |
| **app/dashboard/stats/StatsPage.module.css** | **Reduce section title "corporateness"** | **medium** | Section titles use `text-transform: uppercase; letter-spacing: var(--tracking-wider);` — reads as 2018 enterprise SaaS. | Remove `text-transform: uppercase`. Switch to `letter-spacing: var(--tracking-tight)`. Sentence case + tight tracking is the modern pattern (Linear, Notion, Raycast). Keep `font-weight: var(--font-semibold)` and `color: var(--text-secondary)`. |
| **PactsPageClient.js:161, GroupsPageClient.js:214, DashboardClient.js:373** | **Upgrade empty state SVG illustrations** | **medium** | Current SVGs are geometric primitives: concentric circles (Pacts), overlapping circles (Groups), lightning bolt (Dashboard). The brand-voice copy is excellent ("Your future self is judging you") but illustrations don't match that energy. | Replace with more expressive hand-drawn-style SVGs: Pacts → target with arrow hitting bullseye, Groups → two people high-fiving, Dashboard → rocket launching, Stats → mountain with flag. Use 2-color palette: `var(--accent-primary)` for stroke + `rgba(var(--accent-primary-rgb), 0.08)` for fill. Keep `ambientFloat` animation — it works beautifully. Source: hand-draw in Figma or adapt from unDraw. |
| **app/dashboard/groups/GroupsPageClient.js + GroupsPage.module.css** | **Add visual hierarchy to groups page** | **medium** | All group cards look identical — no owned/joined distinction, no pinned groups, no activity preview. Group icon colors are hash-derived randoms instead of the 7 accent palettes. Weakest page at 6.8/10. | (1) Add "Your Groups" / "Joined Groups" section headers: `font-size: var(--text-lg); font-weight: var(--font-semibold);` (2) Show last activity snippet per card (truncated, `var(--text-sm)`, `color: var(--text-tertiary)`). (3) Replace hash-derived colors with user-selectable accent palettes from `@/lib/accentColors`. (4) Add `cardHover` animation from `@/lib/animations`. (5) Owner badge: small crown icon in `var(--accent-primary)`. |
| **components/OnboardingChecklist.js + OnboardingChecklist.module.css** | **Polish onboarding checklist** | **low** | Progress ring track uses hardcoded opacity. Step items lack hover state. Dismiss button has no tap animation. Completed state uses flat green. | (1) Ring track: `stroke: var(--border-subtle)` instead of hardcoded opacity. (2) Step items: `background: var(--surface-2)` on hover. (3) Add `buttonTap` animation to dismiss button. (4) Completed card: use `background: var(--gradient-success)` instead of flat green. |
| **Brand personality** | **Add micro-copy to success/error states** | **low** | Pact completion shows generic checkmark. Streak milestones lack personality. Form validation errors are functional but sterile. | Pact complete: "Future you says thanks." Streak 7 days: "You're on a roll." Dashboard greeting rotation: "Back already? Respect." / "Day [N] of not procrastinating." / "Your streak is watching." Past deadline validation: "That deadline is in the past. Time travel isn't a feature (yet)." Celebration escalation: Level 5 → confetti, Level 10 → confetti + `celebrationGlow`, Level 25 → special badge with `streakCelebration`. |

**Design System Additions Recommended:**

New CSS variables for `globals.css`:
- `--shadow-streak: 0 0 30px rgba(var(--streak-color-rgb), 0.2)` — dedicated streak glow
- `--surface-onboarding: rgba(var(--accent-primary-rgb), 0.04)` — subtle accent tint for onboarding cards
- `--gradient-streak: linear-gradient(135deg, rgba(var(--streak-color-rgb), 0.1), rgba(var(--accent-celebration-rgb), 0.06))`
- `--text-hero: clamp(2.5rem, 5vw, 3.5rem)` — dedicated dashboard hero stat size

New animation presets for `lib/animations.js`:
- `staggerCards` — 0.06s delay for card grids (slower than list stagger)
- `countUp` — animate number from 0 to target for streak counter / XP bar
- `cardReveal` — combined `fadeInUp` + `scale(0.97→1)` for new card additions
- `hoverLift` — `translateY(-2px)` + `shadow-card-hover` for standardized card hover

**Overall frontend score: 7.6/10** (up from 7.3 yesterday). The two Day-2 blockers (screenshot placeholder + streak hero) are the difference between 7.6 and 8.2.

---

## Automated Fixes Applied

> Easy fixes that were committed in branch `fix/morning-triage-2026-03-25`.

| File | Change | Lines Modified |
|------|--------|----------------|
| lib/gamification.js | Add error check on `createNotification()` return value in achievement unlock flow, consistent with pattern in partnerships.js | 2 |

---

## Skipped — Needs Manual Review

- components/ActivityItem.js:L56 — Dynamic CSS class construction is fragile but functional; fix requires understanding all possible `actionInfo.color` values and redesigning the class lookup. Not broken, just not ideal.
- app/dashboard/groups/[id]/GroupDetailClient.js:L119-142 — Sequential owner profile fetch could be merged with member profile fetch, but requires reordering the data flow logic (members/tasks → profiles → focus sessions). Touches ~30+ lines across the function.
