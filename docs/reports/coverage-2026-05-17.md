# Coverage Report — 2026-05-17

## Summary

| Metric | Before | After |
|--------|--------|-------|
| Statements | 79.86% | 89.05% |
| Branches | 69.44% | 75.84% |
| Functions | 67.1% | 75.58% |
| Lines | 83.33% | 92.12% |

Total tests: 187 → 229.

## Week-over-Week

Compared to last report (2026-04-12):

- **Statements:** 74.9% → 79.86% (before this run) → 89.05% (after)
- **Lines:** 78.05% → 83.33% (before) → 92.12% (after)

Files newly covered this week:
- `lib/comments.js` — 0% → 100% statements
- `lib/nudges.js` — 0% → 100% statements
- `lib/cronAuth.js` — 0% → 92.3% statements
- `lib/streaks.js` — 49.05% → 91.5% statements

Files that remain untested across multiple weeks:
- `lib/onboarding.js` — still 0% (requires `.upsert()` mock support)
- `lib/partnerships.js` — still 0% (requires `.or()` mock support)
- `lib/confetti.js` — 0% (client-only, DOM-heavy, out of scope)
- `lib/sounds.js` — 0% (Web Audio API, out of scope)
- `lib/email.js` — 0% (depends on Resend import, out of scope for unit tests)

## Files Changed (last 7 days)

No `lib/` files were changed in the last 7 days. Focused on lowest-coverage files instead.

## New Tests Written

- `tests/unit/lib/streaks.test.js` — extended with 13 new tests (total 20), covers: `formatDateInTimezone`, `getHourInTimezone`, `getActivityHeatmap` (plus existing `formatUTCDate`, `calculateStreak`)
- `tests/unit/lib/comments.test.js` — 14 tests, covers: `getComments`, `getBatchCommentCounts`, `postComment`, `deleteComment`
- `tests/unit/lib/nudges.test.js` — 4 tests, covers: `sendNudge` (happy path, auth failure, rate limit, insert error)
- `tests/unit/lib/cronAuth.test.js` — 6 tests, covers: `verifyCronSecret` (valid token, missing header, missing env, wrong token, length mismatch, empty header)

## Skipped (out of scope)

- `lib/FocusContext.js`, `lib/NotificationContext.js`, `lib/KeyboardShortcutsContext.js` — React context providers.
- `lib/animations.js` — pure animation data presets, no logic to test.
- `lib/supabase/*` — Supabase client configuration.
- `lib/pactTemplates.js`, `lib/accentColors.js` — already at 100% coverage.
- `lib/confetti.js` — uses `'use client'`, DOM manipulation, `requestAnimationFrame`. Not unit-testable.
- `lib/sounds.js` — Web Audio API. Not unit-testable without full browser environment.
- `lib/email.js` — depends on `Resend` third-party import. Better suited for integration tests.
- `lib/onboarding.js` — requires `.upsert()` mock support (deferred).
- `lib/partnerships.js` — requires `.or()` and multi-chain builder states (deferred).

## Notes

- Added `.limit()` to `tests/setup/supabase-mock.js` chain methods to support testing `nudges.js`. This unblocks nudge testing which was deferred last week.
- `cronAuth.js` uses `crypto.timingSafeEqual` which works correctly in the Node test environment. Line 36 (the `timingSafeEqual` branch for same-length wrong tokens) is covered.
- `streaks.js` coverage went from 49% to 91.5%. The remaining uncovered lines are `formatLocalDate` (unexported helper, only used for display) and `getActivityLevel` boundary level 4 (only reachable with score > 5 which requires specific mock sequencing).
- `comments.js` reached 100% statement coverage. Branch coverage at 82% is due to the notification path in `postComment` (dynamic import of notifications module).
