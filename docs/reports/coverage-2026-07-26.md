# Coverage Report — 2026-07-26

## Summary

| Metric | Before | After |
|--------|--------|-------|
| Statements | 89.56% | 93.71% |
| Branches | 77.41% | 81.96% |
| Functions | 79.41% | 97.05% |
| Lines | 92.49% | 95.22% |

Total tests: 265 → 286 (+21 new tests).

## Week-over-Week (vs 2026-06-21)

| Metric | 2026-06-21 | 2026-07-26 | Change |
|--------|-----------|-----------|--------|
| Statements | 89.56% | 93.71% | +4.15% |
| Branches | 77.41% | 81.96% | +4.55% |
| Functions | 79.41% | 97.05% | +17.64% |
| Lines | 92.49% | 95.22% | +2.73% |
| Test count | 265 | 286 | +21 |

Files that were under-covered last report and are now fully covered:
- `lib/notifications.js` — 87.27% → 100% (statements)
- `lib/activity.js` — 77.58% → 97.41% (statements), 29.16% → 100% (functions)

Files with improved coverage:
- `lib/streaks-advanced.js` — 84.12% → 88.09% (statements), functions now 100%

## Files Changed (last 7 days)

No `lib/` files were modified in the last 7 days. Fell back to lowest-coverage files overall.

## New Tests Written

- `tests/unit/lib/activity.test.js` — 11 new tests, covers: getGroupActivity (happy path with profiles/reactions, fallback user, custom limit/offset), getAllActivity (happy path, test data filtering via title and name, limit after filtering), getGroupStats (full stats computation, zero completions, 100% completion rate)
- `tests/unit/lib/notifications.test.js` — 5 new tests, covers: getNotifications (success, null data, DB error, custom limit, default limit)
- `tests/unit/lib/streaks-advanced.test.js` — 5 new tests, covers: FREEZE_COOLDOWN_DAYS constant, awardStreakFreeze (profile not found, capped at max, successful award via RPC, RPC failure, fallback calculation)

## Skipped (out of scope)

- `lib/FocusContext.js`, `lib/NotificationContext.js`, `lib/KeyboardShortcutsContext.js` — React context providers
- `lib/animations.js` — pure animation data presets, no logic to test
- `lib/supabase/*` — Supabase client configuration
- `lib/confetti.js` — relies on DOM APIs; browser-only with no testable pure logic
- `lib/sounds.js` — relies on Web Audio API; browser-only with no testable pure logic
- `lib/email.js` — core logic is in non-exported functions; the only export requires a real Resend client
- `lib/useKeyboardShortcuts.js`, `lib/useModalScrollLock.js` — React hooks, out of scope

## Notes

- The biggest improvement was in function coverage (+17.64%), driven primarily by `activity.js` going from 29.16% to 100% functions covered. The `getGroupActivity`, `getAllActivity`, and `getGroupStats` functions had only empty/error-path tests previously; they now have full happy-path tests using `mockReturnValueSequence` to simulate multi-query flows.
- `notifications.js` reached 100% across all metrics by adding tests for the previously untested `getNotifications` function.
- `awardStreakFreeze` was previously only exercised indirectly through `updateStreakOnCompletion`; it now has dedicated unit tests covering all branches (profile missing, capped at max, RPC success, RPC failure).
- Remaining uncovered branches in `activity.js` (77.52% branches) are due to the `fetchProfilesMap` error throw path and the `getBatchReactions` auth-error fallback — both would require more complex multi-builder mock setups that risk test fragility.
- `streaks-advanced.js` still has some uncovered lines (355-356, 377) in the `updateStreakOnCompletion` function's dynamic-import notification paths, which are difficult to test in isolation due to `await import()` within try/catch blocks.
