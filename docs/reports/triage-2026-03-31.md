# LockIn Morning Triage — 2026-03-31

**Generated:** 2026-03-31 (automated)
**Findings:** 13 from bug-hunter + frontend-auditor pending = 13+ total
**Cross-referenced:** 6 still open, 1 already fixed, 0 cannot verify
**Cowork visual findings enriched:** 25 findings available from Notion

---

## Progress Since Yesterday
- Last triage report: 2026-03-28 (no report for 03-29 or 03-30)
- Items from 03-28 triage: 2 open, 1 fixed (deadline validation), 1 still open (ActivityFeed race condition)
- Previous triage branch `fix/morning-triage-2026-03-30` had 4 fixes but was **never merged** to main
- Commits since 03-28: 4 (all on unmerged triage branch)
- Cowork visual findings available: yes (25 findings from Notion, 2026-03-29 to 2026-03-31)

## Vercel Health
- **Status:** All deployments healthy
- **Last production deploy:** 3 days ago (Ready, 38s build)
- **Recent preview deploys:** 6 in last 3 days, all Ready
- **No failures, no 500s detected**

---

## Already Fixed

| File | Line | Description |
|------|------|-------------|
| components/CreatePactModal.js | L146 | Past-deadline validation — fixed in commit `4ce1940` on main |

---

## Still Open — Carried Over

> Present in previous triage reports and still unresolved on main.

| Severity | File | Line | Description | Fix Suggestion |
|----------|------|------|-------------|----------------|
| high | app/dashboard/pacts/PactsPageClient.js | L40 | `fetchPacts` useCallback depends on entire `user` object instead of `user.id`, causing unnecessary re-renders and refetches | Change `[supabase, user]` to `[supabase, user.id]` |
| medium | components/ActivityFeed.js | L27-44 | `loadActivities` race condition — rapidly switching groups can cause stale responses to overwrite newer data. No abort controller or stale-request guard. | Add abort controller or request-id ref to discard stale responses |
| medium | app/dashboard/focus/FocusPageClient.js | L63 | `fetchSessions` useCallback missing `user.id` in dependency array | Change `[supabase]` to `[supabase, user.id]` |
| medium | components/CreateGroupModal.js | L8 | `modalOverlay` imported from animations but never used | Remove from import |
| medium | components/CreateTaskModal.js | L9 | `modalOverlay` imported from animations but never used | Remove from import |

---

## Still Open — New

| Severity | File | Line | Description | Fix Suggestion |
|----------|------|------|-------------|----------------|
| high | app/api/cron/send-reminders/route.js | L42-74 | N+1 query pattern: sequential email sends + individual reminder_logs inserts per pact. Can exceed function timeout with many pacts. | Batch reminder_logs inserts; use Promise.allSettled for parallel email sends |
| medium | app/dashboard/stats/StatsPageClient.js | L73-77 | `focus_sessions` query uses `.select('*')` with no `.limit()`. Power users with hundreds of sessions fetch entire table. | Add `.limit(1000)` or use server-side aggregates |
| medium | components/LandingPageClient.js | L399 | Hardcoded hex color values for avatar stack backgrounds instead of CSS variables | Replace with CSS variables from globals.css or accent palette |
| medium | components/FocusTimer.js | L71-73 | Hardcoded hex colors in SVG gradient stops | SVG `stopColor` doesn't support CSS vars in all browsers — add comment explaining why |
| medium | app/share/streak/ShareStreakClient.js | L7 | `createClient` imported from supabase/client but never used | Remove unused import |
| low | app/dashboard/DashboardLayout.js | L49 | `console.error` in client component sign-out handler | Acceptable for debugging; consider toast notification |
| low | app/dashboard/DashboardClient.js | L73 | `console.error` in streak fetch catch block | Consider user-facing error state |
| low | app/globals.css | L282 | `overflow-x: hidden` in `@supports not (overflow: clip)` fallback | Intentional fallback, no action needed |

---

## ⚠️ Security — RLS / Database Access

Supabase MCP was unavailable during this triage run (permission errors on all queries). Unable to verify RLS status. Previous findings from 2026-03-29 memory:
- **CRITICAL (unverified):** `get_user_group_ids(uid)` function has no auth check and is granted to PUBLIC
- **HIGH (unverified):** 8 SECURITY DEFINER functions granted to PUBLIC
- **MEDIUM (unverified):** 8 tables have authenticated DELETE grants but no DELETE policies

**Security code audit: CLEAN.** All cron routes use `verifyCronSecret()` with timing-safe comparison. Auth callback has open-redirect protection. No XSS vectors, SQL injection, or exposed secrets found.

---

## Cannot Verify

| File | Issue | Reason |
|------|-------|--------|
| Database RLS policies | RLS status, policy counts, table security | Supabase MCP returned permission errors for all queries |

---

## Automated Fixes Applied

| File | Change | Lines Modified |
|------|--------|----------------|
| app/dashboard/pacts/PactsPageClient.js | Use `user.id` instead of `user` object in useCallback dependency array | 1 |
| app/dashboard/focus/FocusPageClient.js | Add missing `user.id` to fetchSessions useCallback dependency array | 1 |
| components/CreateGroupModal.js | Remove unused `modalOverlay` import | 1 |
| components/CreateTaskModal.js | Remove unused `modalOverlay` import | 1 |
| app/share/streak/ShareStreakClient.js | Remove unused `createClient` import | 1 |

---

## Skipped — Needs Manual Review

- **components/ActivityFeed.js:L27-44** — Race condition fix requires restructuring useEffect/useCallback with abort controller; touches async data flow (>30 lines, architectural decision needed)
- **app/api/cron/send-reminders/route.js:L42-74** — N+1 query in cron route; excluded from easy fixes (cron route policy)
- **app/dashboard/stats/StatsPageClient.js:L73-77** — Unbounded query needs product decision on limit vs aggregation approach
- **components/LandingPageClient.js:L399** — Hardcoded hex values; CSS variable replacement needs design review for avatar stack
- **components/FocusTimer.js:L71-73** — SVG gradient hex colors; CSS variable support in SVG is browser-dependent
