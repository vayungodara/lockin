# Coverage Report — 2026-06-28

## Summary

| Metric | Before | After |
|--------|--------|-------|
| Statements | 89.56% | 93.23% |
| Branches | 77.41% | 83.06% |
| Functions | 79.41% | 86.27% |
| Lines | 92.49% | 95.49% |

Total tests: 265 → 293 (+28 new tests).

## Week-over-Week (vs 2026-06-21)

| Metric | 2026-06-21 | 2026-06-28 | Change |
|--------|-----------|-----------|--------|
| Statements | 89.56% | 93.23% | +3.67% |
| Branches | 77.41% | 83.06% | +5.65% |
| Functions | 79.41% | 86.27% | +6.86% |
| Lines | 92.49% | 95.49% | +3.00% |
| Test count | 265 | 293 | +28 |

Files with improved coverage:
- `lib/notifications.js` — 87.27% → 100% statements (getNotifications was untested)
- `lib/reactions.js` — 90.47% → 98.41% statements, 81.25% → 93.75% branches
- `lib/activity.js` — 29.16% → 54.16% functions (getGroupStats happy path now covered)
- `lib/streaks-advanced.js` — 84.12% → 90.47% statements, 74.11% → 85.88% branches

Files that remain under 80% on any metric across multiple weeks (highlight):
- `lib/activity.js` — 54.16% functions, 70.78% branches (multi-table mock complexity limits coverage of getGroupActivity and getAllActivity with populated data)

## Files Changed (last 7 days)

No `lib/` files were modified in the last 7 days. Fell back to lowest-coverage files overall.

## New Tests Written

- `tests/unit/lib/notifications.test.js` — 5 new tests, covers: getNotifications (happy path, empty data, null data, DB error, limit parameter)
- `tests/unit/lib/activity.test.js` — 3 new tests, covers: getGroupStats happy path with leaderboard computation, sorting by completions, "Unknown" fallback for missing profiles
- `tests/unit/lib/streaks-advanced.test.js` — 15 new tests, covers: FREEZE_COOLDOWN_DAYS export, checkStreakAtRisk error handling, getStreakFreezeStatus nextFreezeEarned hints (4 streak levels + error), awardStreakFreeze direct tests (profile not found, max cap, success, RPC error, fallback count), applyStreakFreeze RPC edge cases (success:false response, network throw, RPC freezesRemaining)
- `tests/unit/lib/reactions.test.js` — 5 new tests, covers: getBatchReactions unknown activity_id handling + auth error graceful degradation, toggleReaction real DB error (not PGRST116) + delete failure + insert failure

## Skipped (out of scope)

- `lib/FocusContext.js`, `lib/NotificationContext.js`, `lib/KeyboardShortcutsContext.js` — React context providers
- `lib/animations.js` — pure animation data presets, no logic to test
- `lib/supabase/*` — Supabase client configuration
- `lib/confetti.js` — relies on DOM APIs; browser-only with no testable pure logic
- `lib/sounds.js` — relies on Web Audio API; browser-only with no testable pure logic
- `lib/email.js` — core logic in non-exported functions; only export requires real Resend client
- `lib/useKeyboardShortcuts.js`, `lib/useModalScrollLock.js` — React hooks, out of scope

## Notes

- `notifications.js` reached 100% on all metrics — `getNotifications` was the only untested function and is now fully covered.
- `activity.js` function coverage improved from 29% to 54% by adding `getGroupStats` happy-path tests using the per-table mock pattern (`createTableMock`). The remaining uncovered functions are `isTestActivity` (called indirectly inside `getAllActivity`) and `fetchProfilesMap` (internal helper). Reaching them requires populated multi-table data flowing through `getAllActivity`/`getGroupActivity`, which is complex with the current mock infrastructure but does not block further progress.
- `streaks-advanced.js` now has direct tests for `awardStreakFreeze` (previously only tested indirectly through `updateStreakOnCompletion`). The remaining uncovered lines (147, 355-356, 377) are in deeply nested error-recovery paths within `updateStreakOnCompletion`'s milestone-freeze logic.
- `reactions.js` branches improved from 81% to 94% by covering the PGRST500 error path (line 103) and the unknown-activity-id initialization (line 70).
