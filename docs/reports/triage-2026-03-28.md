# LockIn Morning Triage — 2026-03-28

**Generated:** 2026-03-28 08:00 CET (automated)
**Sources:** Morning Report A: present, Morning Report B: present, Frontend Audit: present
**Findings before dedup:** 16 from A + 3 from B = 19 total before merge
**After dedup:** 19 merged findings (no overlapping issues detected)
**Cross-referenced:** 19 verified against repo, 6 already fixed, 1 cannot verify

---

## Already Fixed

> These issues were flagged in today's reports but are no longer present in the current code.

| File | Line | Description |
|------|------|-------------|
| app/api/cron/cleanup/route.js | L42 | Cleanup threshold mismatch — code properly handles session duration via `expectedEnd` calculation using `duration_minutes`; sessions are capped at their expected end time, not a hardcoded threshold. |
| lib/streaks.js | L125, L140 | Math.round date calc concern — dates are parsed from YYYY-MM-DD strings as UTC midnight, so differences are exact multiples of 86400000ms; `Math.round` is a reasonable floating-point safety measure, not a bug. |
| app/api/cron/cleanup/route.js | L30 | Cleanup result tracking — the `results` object properly tracks `overduePacts` and `orphanedSessions` counts with correct fallback to 0. |
| components/MonthlyCalendar.js | L21 | useMemo vs useRef for Supabase client — `useMemo(() => createClient(), [])` is a valid codebase convention also used in ActivityFeed.js; works correctly for stable singleton. |
| components/MonthlyCalendar.js | L47-53 | Stale dataLookup memo — `useMemo` depends on `[heatmapData]` which correctly recomputes when `setHeatmapData` provides a new array reference. |
| components/CreatePactModal.js | L148 | Deadline date not validated — client-side validation exists: `if (deadlineDateTime < new Date()) { setError('Deadline must be in the future'); return; }`. |

---

## Still Open — Carried Over

> Present in today's report AND yesterday's triage. These have persisted at least 2 days.

No carried-over issues (first run — no previous triage report found at `docs/reports/triage-2026-03-27.md`).

---

## Still Open — New Since Yesterday

> Present in today's report but NOT in yesterday's triage (first run — all marked as new).

| Severity | File | Line | Description | Fix Suggestion |
|----------|------|------|-------------|----------------|
| critical | lib/nudges.js | L16-26 | **Nudge Rate Limit Race Condition** — Rate limit check queries for recent nudges then inserts without row locking. Concurrent requests can all pass the 1-per-hour check simultaneously. | Add a unique constraint on `(from_user_id, to_user_id)` with a partial index on `created_at > now() - interval '1 hour'`, or use a Supabase RPC with `SELECT ... FOR UPDATE`. Requires DB schema change. |
| high | components/MonthlyCalendar.js | L28 | **Streak Timezone Bypass** — `calculateStreak(supabase, userId)` called without user timezone parameter, defaults to UTC. Streak display may differ from actual streak for non-UTC users. | Thread user timezone from profile/settings through to `calculateStreak(supabase, userId, timezone)`. Requires plumbing timezone through multiple components. |
| high | lib/partnerships.js | L76-82 | **Partnership Auth Bypass** — `acceptPartnerRequest()` updates status to 'active' filtering only by `id` and `user2_id`, without checking current status is 'pending'. Already-active or declined partnerships can be re-activated. | Add `.eq('status', 'pending')` to the update query chain. **Applied as automated fix.** |
| high | components/ActivityFeed.js | L27-48 | **ActivityFeed Race Condition** — `loadActivities` is called when `groupId` changes but doesn't cancel pending requests. Rapid group switching can cause stale responses to overwrite newer data. | Add a `groupIdRef` to detect stale responses and skip state updates when groupId has changed during fetch. **Applied as automated fix.** |
| medium | next.config.mjs | L10 | **Overly Broad Image Remote** — Pattern `**.googleusercontent.com` matches any subdomain. Should restrict to `lh3.googleusercontent.com` and `lh4.googleusercontent.com` for Google profile images. | Replace `**` with explicit hostnames. Excluded from auto-fix (next.config.mjs changes not allowed). |
| medium | lib/reactions.js | L59-61 | **Silent Auth Failure in Reactions** — `getBatchReactions()` logs warning on auth error but continues with `userId=null`, silently degrading to unauthenticated behavior without caller awareness. | Propagate auth error to caller or set a flag in the return value. Requires understanding of desired behavior for logged-out users. |
| medium | app/api/cron/send-reminders/route.js | L42-74 | **Sequential Email Sending** — Emails sent in `for...of` loop with `await` per email. 100 pacts at 5s each = 500s, risking Vercel cron timeout (60s on Hobby). | Batch with `Promise.allSettled()` in chunks of 10. Excluded from auto-fix (cron route). |
| medium | lib/reactions.js | L23 | **Redundant Auth Calls in Reactions** — `getReactions()` calls `getUser()` per activity item. `getBatchReactions()` exists and calls it once, but individual `getReactions` is still available for use. | Deprecate `getReactions` in favor of `getBatchReactions`, or cache auth result. Requires API refactoring. |
| medium | app/api/cron/check-streak-risk/route.js | L46 | **UTC Hardcode in Streak Risk Cron** — Compares `created_at` against `${today}T00:00:00Z` in UTC. Users in UTC-8 may get false at-risk warnings when their local day hasn't ended. | Use user timezone from profile to determine "today" per user. Excluded from auto-fix (cron route). |
| low | lib/comments.js | L49 | **Comments Error Handling Inconsistency** — `getBatchCommentCounts()` returns `{}` on error while `getComments` returns `{ data: [], error: err }` and `postComment` returns `{ success: false, error: err }`. Inconsistent return contracts. | Standardize all comment functions to return `{ data, error }` shape. Low impact since callers handle both shapes. |
| low | lib/partnerships.js | L19 | **Complex .or() Clause** — Template literal string interpolation in `.or()` filter: `and(user1_id.eq.${userId},user2_id.eq.${targetUserId}),and(...)`. Works but is fragile and hard to read. | Extract to a helper or use two separate queries merged client-side. Style preference, not a bug. |
| low | lib/gamification.js | L44 | **Gamification Error Types** — `awardXP` returns `{ success: false, error: err }` where `err` is the raw caught exception, while callers like `checkPactAchievements` don't surface errors at all. Minor inconsistency in error propagation. | Normalize error returns across gamification functions. Low impact. |

---

## Cannot Verify

> Issues that could not be confirmed or denied due to missing files, vague descriptions, or out-of-range lines.

| File | Issue | Reason |
|------|-------|--------|
| supabase/checkpoint8_complete.sql | Recurrence type mismatch — `monthly` accepted by UI but allegedly rejected by DB CHECK constraint | The SQL file is a migration checkpoint. The actual live DB constraint cannot be verified from the file alone; the constraint may have been updated in a later migration. The UI (CreatePactModal.js) correctly allows all 4 recurrence types including 'monthly'. |

---

## Frontend Audit Suggestions

> Design and CSS recommendations from today's frontend audit email. Not bugs — improvement ideas.
> **Frontend is the developer's favorite area — go in depth here.** For each suggestion, include: what the current code looks like, what the improvement would be, which CSS variables / animation presets from the design system apply, and a concrete before/after snippet if possible. Prioritize visual polish, micro-interactions, and design consistency suggestions highest.

| Component / File | Suggestion | Priority | Current Code | Recommended Change |
|-----------------|------------|----------|--------------|-------------------|
| app/page.module.css `.featureGrid` | **Convert Feature Grid to Bento Layout** — The uniform 2-column grid with identical card sizes is the #1 AI template pattern. Every card has same `var(--space-8)` padding, same 32px icons, same `var(--radius-2xl)`. | high | `.featureGrid { display: grid; grid-template-columns: repeat(2, 1fr); gap: var(--space-6); }` with all `.featureCard` identical. | Make 2 cards span full width with richer content: `.featureCardLarge { grid-column: span 2; padding: var(--space-10); }` using `grid-template-columns: repeat(3, 1fr)` on desktop. Give featured cards (Group Accountability, Activity Feed) a mini-illustration or embedded preview component. Reference: Apple product pages, Raycast homepage. |
| components/LandingPageClient.js | **Inject Brand Voice into Hero Copy** — Feature descriptions ("Make commitments that stick", "Real-time updates") are generic and could be from any productivity app. LockIn's unique angle is social pressure. | high | Generic benefit-focused copy like "See who is pulling their weight" | Sharper, more confrontational copy matching the slogan: "Your friends can see when you flake", "Nothing motivates like public accountability", "Get called out (lovingly) when you miss a deadline". Use `var(--text-lg)` with `font-weight: var(--font-semibold)` for feature headlines. |
| DailySummaryCard.module.css, PactCard.module.css | **Differentiate Card Hover Intensities** — Every card uses identical `translateY(-3px) + var(--shadow-lg)` on hover. Human designers create hierarchy through different hover intensities. | high | Both DailySummaryCard and PactCard: `transform: translateY(-3px); box-shadow: var(--shadow-lg);` | Info cards (DailySummary): `transform: translateY(-1px); box-shadow: var(--shadow-md);` — subtle acknowledgment. Action cards (PactCard): `transform: translateY(-4px); box-shadow: var(--shadow-xl);` — dramatic hover for primary actions. Decorative cards: color shift only via `background: var(--surface-2);` on hover, no elevation. Use `transition: all 0.2s var(--ease-out-quint);` for snappy feel. |
| app/dashboard/Dashboard.module.css | **Add Brand Gradient Accent to Dashboard** — Once logged in, the dashboard looks like a generic SaaS. Only the indigo accent color identifies LockIn. No brand visual signature. | medium | Dashboard header is plain text with no brand element. | Add a 2px gradient line under the greeting: `&::after { content: ''; display: block; height: 2px; width: 60px; background: linear-gradient(135deg, var(--accent-primary), var(--accent-secondary, #7C4DFF), var(--accent-tertiary, #E040CB)); border-radius: var(--radius-full); margin-top: var(--space-2); }`. Reference: Arc Browser colored bars per space. |
| components/FocusTimer.module.css | **Stop Breathe Animation When Paused** — The `breathe` animation (box-shadow pulse at 4s) runs indefinitely even when timer is paused. Should only animate during active focus. | medium | `.timerRing::after { animation: breathe 4s ease-in-out infinite; }` runs always. | Add conditional class: `.timerRingPaused::after { animation-play-state: paused; }` and toggle via `className={isActive ? styles.timerRing : styles.timerRingPaused}`. The `ambientBreathing` preset from `@/lib/animations` could also be used with Framer Motion's `animate` prop conditioned on timer state. Reference: Forest App only animates during active focus. |
| app/page.module.css `.screenshotPlaceholder` | **Remove or Replace Screenshot Placeholder** — L135-143 defines a 400px gray box. The interactive mock dashboard exists but this placeholder class is still in use (LandingPageClient.js:347). Either replace with a real dashboard screenshot or remove the element entirely. | medium | `.screenshotPlaceholder { height: 400px; background: var(--bg-secondary); }` rendered as empty gray box. | Replace the `<div className={styles.screenshotPlaceholder}>` in LandingPageClient.js with an `<Image>` component showing a real dashboard screenshot. Use `border-radius: var(--radius-xl); box-shadow: var(--shadow-2xl); overflow: hidden;` for polish. Use `fadeInScale` from `@/lib/animations` for entrance. |
| app/dashboard/settings/SettingsPage.module.css | **Add Slider Track Fill** — Range input sliders show no visual indication of the current value along the track. | medium | `.slider { background: var(--surface-3); }` — uniform track color. | Use `accent-color: var(--accent-primary)` on the `<input type="range">` for a quick native solution. For more control: set `background: linear-gradient(to right, var(--accent-primary) 0%, var(--accent-primary) ${pct}%, var(--surface-3) ${pct}%, var(--surface-3) 100%)` dynamically. |
| app/dashboard/stats/ | **Stats Page Achievement Showcase Redesign** — The stats page reads like a spreadsheet. Flat numbers with dividers, no visual interest. Weakest page (7/10 score). | low | Basic `display: grid; grid-template-columns: repeat(2, 1fr)` stat cards with plain numbers. | Add sparkline mini-charts (7-day trend) via inline SVGs. Add trend arrows with `var(--success)` / `var(--danger)` color. Make streak card visual with icon scaling: `transform: scale(${Math.min(1 + streak * 0.02, 1.5)})`. Use `ambientGlow` from `@/lib/animations` on active streak card. Add `--gradient-streak: linear-gradient(135deg, #FF8C32, #F5A623, #FFD700)` to globals.css for warm gold streak celebrations. |
| Dashboard, DailySummaryCard, Sidebar | **Visual Brand Signature** — No visual manifestation of LockIn's "social pressure" angle. Needs urgency, tension, peer visibility cues in the visual language. | low | Clean generic SaaS appearance with indigo accent only. | Brand texture ideas: (1) Lock icon micro-interaction on pact creation. (2) Urgency hash pattern on deadline cards: `background-image: repeating-linear-gradient(135deg, transparent, transparent 10px, rgba(var(--danger-rgb), 0.03) 10px, rgba(var(--danger-rgb), 0.03) 20px)`. (3) "X people locked in today" counter with `ambientFloat` animation. (4) Sidebar logo `ambientGlow` on streak milestones (7, 30, 100 days). |

---

## Automated Fixes Applied

> Easy fixes that were committed in branch `fix/morning-triage-2026-03-28`.

| File | Change | Lines Modified |
|------|--------|----------------|
| lib/partnerships.js | Added `.eq('status', 'pending')` to `acceptPartnerRequest()` update query to prevent re-activating declined/active partnerships | 1 |
| components/ActivityFeed.js | Added `groupIdRef` stale-request guard to prevent race condition when rapidly switching groups | 3 |

---

## Skipped — Needs Manual Review

> Issues that qualify for a fix but were skipped because they exceed the automated fix criteria.

- lib/nudges.js:16-26 — Nudge rate limit race condition requires DB-level locking or unique constraint (schema change)
- components/MonthlyCalendar.js:28 — Streak timezone bypass requires threading user timezone through multiple components
- next.config.mjs:10 — Overly broad image remote pattern; next.config.mjs modifications excluded from auto-fix
- lib/reactions.js:59-61 — Silent auth failure; changing behavior could break feed for edge cases, requires understanding business logic
- app/api/cron/send-reminders/route.js:42-74 — Sequential email sending; cron route excluded from auto-fix
- lib/reactions.js:23 — Redundant auth calls; requires API refactoring across multiple callers
- app/api/cron/check-streak-risk/route.js:46 — UTC hardcode; cron route excluded from auto-fix
- lib/comments.js:49 — Error handling inconsistency; would change return type contracts for multiple consumers
- lib/partnerships.js:19 — Complex .or() clause; style preference, not a bug
- lib/gamification.js:44 — Gamification error types; minor, non-breaking inconsistency
