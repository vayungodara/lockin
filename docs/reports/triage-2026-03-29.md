# LockIn Morning Triage — 2026-03-29

**Generated:** 2026-03-29 08:00 CET (automated)
**Sources:** Morning Report A: present, Morning Report B: present, Frontend Audit: present
**Findings before dedup:** [20 from A] + [4 from B] = 24 total before merge
**After dedup:** 24 merged findings
**Cross-referenced:** 24 verified against repo, 10 already fixed, 1 cannot verify

---

## Already Fixed

> These issues were flagged in today's reports but are no longer present in the current code.

| File | Line | Description |
|------|------|-------------|
| lib/partnerships.js | L87 | Partnership Auth Bypass — `acceptPartnerRequest()` already has `.eq('user2_id', userId)` constraint preventing bypass. |
| app/api/cron/check-streak-breaks/route.js | L61 | Stale Streak in Break Notification — using old `current_streak` is intentional semantics ("Your X-day streak has ended"). |
| lib/partnerships.js | L84 | Silent Partnership Failure — error from `.single()` is caught and returned as `{ success: false, error: err.message }`. |
| lib/FocusContext.js | L183-186 | Redundant Ref Sync — dual sync (useCallback + useEffect) is intentional to prevent null refs in interval callbacks. |
| lib/comments.js | L49 | Error handling returning `{}` — matches expected return structure for batch operations. |
| lib/streaks.js | L125,140 | Math.round date calc — dates pre-bucketed via `formatDateInTimezone`, eliminating day-boundary rounding errors. |
| app/api/cron/cleanup/route.js | L30 | Cleanup result tracking — `results` object correctly tracks both `overduePacts` and `orphanedSessions`. |
| components/MonthlyCalendar.js | L47-53 | Stale dataLookup memo — dependencies are correct; `[heatmapData]` updates properly. |
| lib/FocusContext.js | L213 | Focus Timer mode useMemo — `mode` in deps is correct; duration refs are also in deps array at L299. |
| supabase/add_monthly_recurrence.sql | — | recurrence_type DB validation — CHECK constraint already present: `IN ('daily', 'weekly', 'weekdays', 'monthly')`. |

---

## Still Open — Carried Over

> Present in today's report AND yesterday's triage. These have persisted at least 2 days.

| Severity | File | Line | Description | Fix Suggestion |
|----------|------|------|-------------|----------------|
| medium | components/ActivityFeed.js | L46 | Race condition: `loadActivities` called on groupId change but pending requests not cancelled. Rapidly switching groups can cause stale data overwrite. PR #38 added realtime subscriptions but no request cancellation. | Add AbortController in useEffect cleanup, or use a stale-request ref pattern to discard outdated responses. |

---

## Still Open — New Since Yesterday

> Present in today's report but NOT in yesterday's triage (or first run).

| Severity | File | Line | Description | Fix Suggestion |
|----------|------|------|-------------|----------------|
| high | lib/reactions.js | L23-24 | Redundant auth: `getReactions()` calls `supabase.auth.getUser()` per activity item. 15 items = 15 auth round-trips. | Cache auth state once at the top of the calling function, pass userId down. |
| high | components/MonthlyCalendar.js | L21 | Supabase client created inside `useMemo(() => createClient(), [])`. Should use `useRef` or module-level singleton for correct React semantics. | Replace `useMemo` with `useRef` pattern: `const clientRef = useRef(null); if (!clientRef.current) clientRef.current = createClient();` |
| high | lib/gamification.js | L46 | Error object not serializable: `return { success: false, error: err }` — Error objects serialize to `{}`. | Change `error: err` to `error: err.message`. |
| medium | next.config.mjs | L11 | Overly broad image remote: `**.googleusercontent.com` matches any subdomain including attacker-controlled. | Restrict to `lh3.googleusercontent.com` and `lh4.googleusercontent.com` only. |
| medium | lib/nudges.js | L24 | Rate limit race condition: check-then-insert without row lock. Concurrent requests can bypass 1-per-hour limit. | Add a UNIQUE constraint with timestamp window, or use a DB function with row lock. |
| medium | components/MonthlyCalendar.js | L28 | Streak timezone bypass: `calculateStreak()` called without user timezone, defaults to UTC. Display incorrect for non-UTC users. | Pass user's timezone from settings/profile to `calculateStreak()`. |
| medium | app/api/cron/cleanup/route.js | L42 | Cleanup threshold mismatch: hardcoded 1-hour threshold doesn't account for actual session duration. 60-90 min old sessions with 25-min Pomodoro incorrectly cleaned. | Derive threshold from configured session duration, or increase to 2 hours. |
| medium | app/api/cron/check-streak-risk/route.js | L46 | UTC hardcode: `new Date().toISOString().split('T')[0]` gives UTC date. Non-UTC users get false at-risk warnings. | Use user's timezone when comparing activity dates. |
| medium | components/CreatePactModal.js | L99 | Stale `today` variable: pinned when modal opens via `useMemo([isOpen])`. If modal stays open past midnight, template selection shows stale date. | Remove useMemo wrapper or add a date refresh mechanism. |
| medium | app/api/cron/send-reminders/route.js | L42-75 | Sequential email sending: `for...of` with `await` sends emails serially. 100 pacts at 5s each = 500s, risking Vercel 60s timeout. | Use `Promise.allSettled()` for parallel sends with batching. |
| low | lib/reactions.js | L60 | Silent auth failure in `getBatchReactions`: `console.warn()` on error but continues with `userId = null`. Error swallowed. | Propagate auth failure to caller or retry with backoff. |
| low | lib/partnerships.js | L19 | Complex `.or()` clause with string interpolation is fragile and error-prone. | Refactor to separate queries or a database function for bidirectional lookup. |

---

## Cannot Verify

> Issues that could not be confirmed or denied due to missing files, vague descriptions, or out-of-range lines.

| File | Issue | Reason |
|------|-------|--------|
| lib/gamification.js | XP Drift — some XP award paths may bypass `xp_events` logging | `awardXP()` delegates to `supabase.rpc('award_xp')`. The RPC function's implementation lives in the database, not in repo code. Cannot verify without inspecting the DB function definition. |

---

## Frontend Audit Suggestions

> Design and CSS recommendations from today's frontend audit email. Not bugs — improvement ideas.
> **Frontend is the developer's favorite area — go in depth here.**

| Component / File | Suggestion | Priority | Current Code | Recommended Change |
|-----------------|------------|----------|--------------|-------------------|
| Sidebar.module.css:216 | Hardcoded box-shadow color on `.miniTimer` | high | `box-shadow: 0 4px 12px rgba(99,102,241,0.3)` | `box-shadow: 0 4px 12px rgba(var(--accent-primary-rgb), 0.3)` — This makes the mini timer glow respect the user's chosen accent palette. Currently, switching to "ocean" or "emerald" accent still shows indigo glow. |
| NotificationBell.module.css:91 | Hardcoded hover background on `.markAllBtn` | high | `background: rgba(99,102,241,0.1)` | `background: rgba(var(--accent-primary-rgb), 0.1)` — Same issue: accent color switching doesn't affect notification bell. |
| GroupDetail.module.css:76-91 | Hardcoded colors in `.lockedInBadge` | high | `rgba(139,92,246,0.5)` and similar purple hardcodes | Replace with `rgba(var(--accent-primary-rgb), 0.5)`. The locked-in badge should pulse in the user's accent color, not permanent purple. |
| FocusTimer.js:71-73 | SVG gradient uses hardcoded hex stops | high | `<stop stopColor="#6366F1"/>`, `<stop stopColor="#8B5CF6"/>`, `<stop stopColor="#D946EF"/>` | **Option A (recommended):** Use CSS `background: var(--gradient-primary)` with `mask` on a wrapper div instead of inline SVG gradient. **Option B:** Read accent vars via `getComputedStyle(document.documentElement).getPropertyValue('--accent-primary')` and inject dynamically. This is the most visible hardcoded color — the focus timer ring ignores accent themes entirely. |
| ParticleBackground.js:77-78 | Hardcoded particle RGB values | medium | `'139, 92, 246'` hardcoded string | Extract from `getComputedStyle(document.documentElement).getPropertyValue('--accent-primary-rgb')`. Also add `prefersReducedMotion()` check from `@/lib/animations` and reduce particle count on mobile. |
| DailySummaryCard.module.css:15 | Info card uses same hover as action cards | high | `transform: translateY(-3px)` + `var(--shadow-lg)` on hover | Change to `translateY(-1px)` + `var(--shadow-md)` — info cards should have subtle hover. Keep `translateY(-4px)` + `var(--shadow-xl)` for action cards (PactCard). Feature/decorative cards should use color shift only, no elevation. This breaks the "uniform AI template" pattern where every card hovers identically. Reference: Notion uses barely-there hover on info blocks, stronger on actionable items. |
| app/page.module.css:135 | Dead `.screenshotPlaceholder` CSS | medium | Two `.screenshotPlaceholder` blocks at L135-143 and L1060 — completely unused since interactive mock dashboard replaced it | Delete both blocks. Dead CSS is a code smell. ~15 lines removed. |
| FocusTimer.module.css:75 | Breathe animation runs when timer is paused | medium | `.timerRing::after` animation runs continuously regardless of timer state | Add `.timerRingPaused::after { animation: none; }` in CSS, toggle class in `FocusTimer.js` based on `isRunning`. Reference: Forest App only animates its tree during active focus. Use `ambientBreathing` from `@/lib/animations` conditionally: `animate={isRunning ? ambientBreathing : {}}`. |
| LandingPageClient.js:395-399 | Social proof avatars use hardcoded hex colors | medium | `style={{ backgroundColor: color }}` with array `['#5B5EF5', '#7C4DFF', '#E040CB', '#00C9A7', '#FF6B6B']` | Use CSS variables: `var(--accent-primary)`, `var(--accent-secondary)`, `var(--accent-tertiary)` for first three, then brand gradient colors. Replace plain circles with initial letters or emoji. Add `ambientFloat` animation from `@/lib/animations` on the stack container. |
| TaskCard.module.css:235-237 | Loading overlay uses hardcoded RGBA | medium | `rgba(255,255,255,0.6)` light, `rgba(0,0,0,0.4)` dark — not theme-aware | Create `--overlay-bg` token in `globals.css`: light `rgba(255,255,255,0.6)`, dark `rgba(0,0,0,0.4)` under `[data-theme="dark"]`. Then use `background: var(--overlay-bg)` in TaskCard. |
| SettingsPageClient.js | Settings slider lacks track fill | medium | Range input shows default browser styling with no filled portion | Set `background: linear-gradient(to right, var(--accent-primary) 0%, var(--accent-primary) ${pct}%, var(--surface-3) ${pct}%, var(--surface-3) 100%)` dynamically. Or simpler: add `accent-color: var(--accent-primary)` to the input range. Reference: Vercel dashboard sliders show filled portion. |
| Dashboard.module.css | No brand visual signature in dashboard header | low | Plain greeting text with no brand accent | Add after greeting: `background: var(--gradient-primary); height: 2px; border-radius: 1px; margin-top: var(--space-3);` — a thin gradient accent line. Reference: Arc Browser uses a colored bar at the top of each space. |
| app/page.module.css → .featureGrid | Feature grid still uniform 2-column layout | low | `grid-template-columns: repeat(2, 1fr)` with `isHero` span-2. All non-hero cards identical height/padding. | Convert to 3-column bento: `grid-template-columns: repeat(3, 1fr)`. Hero cards: `grid-column: span 2; grid-row: span 2`. Vary padding and content density per card type. Reference: Apple.com product pages, Raycast homepage. |

---

## Automated Fixes Applied

> Easy fixes that were committed in branch `fix/morning-triage-2026-03-29`.

| File | Change | Lines Modified |
|------|--------|----------------|
| lib/gamification.js | Change `error: err` to `error: err.message` for proper serialization | 1 |

---

## Skipped — Needs Manual Review

> Issues that qualify for a fix but were skipped because they exceed the automated fix criteria.

- lib/reactions.js:L23-24 — Redundant auth calls require restructuring auth caching pattern across multiple functions (>30 lines, architectural decision).
- components/MonthlyCalendar.js:L21 — `useMemo` → `useRef` for Supabase client is a refactoring change, not a targeted bug fix.
- next.config.mjs:L11 — Overly broad image remote. Criteria excludes changes to `next.config.mjs`.
- lib/nudges.js:L24 — Rate limit race condition requires DB-level fix (unique constraint or row lock), not a simple code change.
- components/MonthlyCalendar.js:L28 — Streak timezone bypass requires passing timezone from user settings; architectural decision about where timezone is stored/accessed.
- app/api/cron/cleanup/route.js:L42 — Cron route excluded from automated fixes per criteria.
- app/api/cron/check-streak-risk/route.js:L46 — Cron route excluded from automated fixes per criteria.
- components/CreatePactModal.js:L99 — Stale `today` fix requires decision on whether date should be pinned (current behavior) or dynamic; design intent unclear.
- app/api/cron/send-reminders/route.js:L42-75 — Cron route excluded from automated fixes per criteria. Also a significant refactor (serial → parallel with error handling).
- components/ActivityFeed.js:L46 — Race condition fix requires restructuring useEffect with AbortController; >30 lines of conceptual change.
- lib/partnerships.js:L19 — Complex `.or()` clause refactor is a structural change, not a targeted fix.
