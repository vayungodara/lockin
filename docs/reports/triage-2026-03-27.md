# LockIn Morning Triage — 2026-03-27

**Generated:** 2026-03-27 08:00 CET (automated)
**Sources:** Morning Report A: missing, Morning Report B: present, Frontend Audit: missing
**Findings before dedup:** [0 from A] + [5 from B] = 5 total before merge
**After dedup:** 5 merged findings
**Cross-referenced:** 5 verified against repo, 1 already fixed, 0 cannot verify

---

## Already Fixed

> These issues were flagged in today's reports but are no longer present in the current code.

| File | Line | Description |
|------|------|-------------|
| components/PactCard.js | L79-144 | `onUpdate` was reported as firing before `Promise.race` settles, but the current code calls `onUpdate` at L162 — after `await Promise.race(...)` at L151. Parent state and DB are now consistent before the callback fires. |

---

## Still Open — Carried Over

> Present in today's report AND yesterday's triage. These have persisted at least 2 days.

No carried-over issues (no previous triage report found for 2026-03-26).

---

## Still Open — New Since Yesterday

> Present in today's report but NOT in yesterday's triage (first run).

| Severity | File | Line | Description | Fix Suggestion |
|----------|------|------|-------------|----------------|
| medium | app/dashboard/groups/[id]/GroupDetailClient.js | — | Kanban board is display-only — tasks are grouped into columns but users cannot drag tasks between them to change status. A Kanban board without drag-and-drop is a filtered list. | Integrate `@dnd-kit/core`: each `.columnContent` div becomes a `Droppable`, each `TaskCard` a `Draggable`. On `onDragEnd`, update task `status` via Supabase. |
| medium | components/ActivityFeed.js | — | Activity feed loads once on mount and paginates via infinite scroll but never subscribes to Supabase real-time changes. Group members don't see each other's activity until manual refresh. | Add `useEffect` with `supabase.channel('group-activity').on('postgres_changes', { event: 'INSERT', schema: 'public', table: 'activity_log' }, handler).subscribe()` and clean up on unmount. |
| medium | lib/streaks.js | L78-84 | `calculateStreak` fetches ALL completed pacts for a user without any limit or pagination. Power users with 1000+ completions will see degraded dashboard load times as the full history is transferred and parsed. | Create a server-side RPC `get_streak_data(user_id, timezone)` that computes streaks in PostgreSQL using indexes on `completed_at` and `user_id`, returning only the three integers. |
| medium | lib/FocusContext.js | L208-237 | Unmount cleanup fires an async IIFE to finalize focus sessions in DB. React doesn't await cleanup functions, so if the component remounts quickly, `cleanupInProgressRef` may still be `true`, causing the second cleanup to bail out — orphaning the session with no `ended_at`. | Either handle orphaned sessions via cron (mark `ended_at = started_at + duration_minutes` where `ended_at IS NULL` and `started_at < now() - interval '1 hour'`), or use `navigator.sendBeacon` for guaranteed delivery on unmount. |

---

## Cannot Verify

> Issues that could not be confirmed or denied due to missing files, vague descriptions, or out-of-range lines.

All issues were verifiable.

---

## Frontend Audit Suggestions

> Design and CSS recommendations from today's frontend audit email. Not bugs — improvement ideas.

Frontend audit email had not arrived at triage time.

---

## Automated Fixes Applied

> Easy fixes that were committed in branch `fix/morning-triage-2026-03-27`.

No automated fixes applied today. All STILL_OPEN issues require architectural decisions, new dependencies, or feature-level work that exceeds easy-fix criteria.

---

## Skipped — Needs Manual Review

> Issues that qualify for a fix but were skipped because they exceed the automated fix criteria.

- `app/dashboard/groups/[id]/GroupDetailClient.js` — Requires new dependency (`@dnd-kit/core` or similar) and significant feature implementation (drag-and-drop handlers, Supabase mutations on drag end)
- `components/ActivityFeed.js` — Adding real-time subscriptions is a new feature touching Supabase channel management and component lifecycle
- `lib/streaks.js:L78` — Optimizing to a server-side RPC requires a new Supabase function and altering the query contract
- `lib/FocusContext.js:L208` — Fix requires architectural decision (cron-based cleanup vs sendBeacon vs other approach)
